# src/main/resources/application.yml 이 참조하는 환경변수 목록.
# (맨 아래 Grafana Cloud 항목만 예외다 — 앱이 아니라 로컬 모니터링 검증 도구가 읽는다.)
# 이 파일을 복사해 레포 루트에 .env로 저장한 뒤 실제 값을 채워 사용한다 (.env는 git에 커밋하지 않는다).
# spring-dotenv 의존성이 .env를 자동으로 읽어 환경변수로 주입하므로,
# 셸에서 직접 export할 필요 없이 ./gradlew bootRun만 실행하면 된다.
# (실제 OS 환경변수가 있으면 그 값이 항상 우선한다.)

# --- Spring Profile ---
# 지정하지 않으면 local 프로필로 뜬다(application.yml의 spring.profiles.default).
# 로컬 개발에서는 비워두면 되고, 배포 서버에서만 prod로 지정한다.
# 배포 서버 적용 절차는 docs/guide/profile.md 참고.
#
# 주의: spring-dotenv가 이 .env를 읽는 시점이 프로필 결정보다 늦을 수 있어, 여기에
# SPRING_PROFILES_ACTIVE를 써도 프로필이 안 잡힐 수 있다. 로컬에서 prod를 흉내내려면
# 셸 환경변수로 직접 주는 편이 확실하다: SPRING_PROFILES_ACTIVE=prod ./gradlew bootRun
# SPRING_PROFILES_ACTIVE=prod

# --- Database (PostgreSQL) ---
DB_URL=jdbc:postgresql://localhost:5432/yourtrip
DB_USERNAME=
DB_PASSWORD=
# Hibernate ddl-auto 정책: none | validate | update | create | create-drop
DB_DDL_AUTO=validate

# --- JWT ---
JWT_SECRET=

# --- Mail (Gmail SMTP) ---
MAIL_EMAIL=
MAIL_PASSWORD=

# --- Kakao (장소 검색용, 로그인은 미사용) ---
KAKAO_API_KEY=

# --- AWS S3 ---
S3_BUCKET=
AWS_ACCESS_KEY=
AWS_SECRET_KEY=

# --- CloudFront (이미지 URL 발급 — 공개 콘텐츠/mycourse Signed URL) ---
# terraform apply 이후 `terraform output`으로 나오는 값을 그대로 채운다 (terraform/README.md 참고).
CLOUDFRONT_DOMAIN=
CLOUDFRONT_KEY_PAIR_ID=
# terraform/README.md 절차로 생성한 cloudfront_private_key.pem의 로컬 경로 (레포에 커밋하지 않음)
CLOUDFRONT_PRIVATE_KEY_PATH=
CLOUDFRONT_DISTRIBUTION_ID=

# --- OpenAI (AI 코스 생성 멀티 에이전트 파이프라인) ---
# application.yml의 llm.openai.api-key가 참조한다(spring.ai.openai.* 가 아니다 —
# 어댑터가 OpenAiChatModel을 직접 조립하므로 설정이 llm.* 하나로 모인다).
#
# 8단계 스위치로 AI 코스 생성이 이 어댑터를 타므로 실질적으로 필수다.
# 비어 있어도 앱은 정상 기동한다 — 그래서 기동과 헬스체크는 통과하고 코스 생성 요청만
# 실패한다. 값이 빠졌을 때 가장 늦게 발견되는 키이므로 주의한다.
# 운영은 이 파일이 아니라 SSM /yourtrip/prod/env/OPENAI_API_KEY 에서 받아간다
# (terraform/prod/README.md).
OPENAI_API_KEY=

# --- Naver 지역검색 API (후보 공급 — 인기 장소 시딩) — NAVER API HUB ---
# application.yml의 naver.client-id / naver.client-secret 이 참조한다(NaverLocalClient).
#
# V1에서 네이버의 용처는 지역검색 하나뿐이다. 슬롯별로 "{area} {searchHint}" 를 sort=comment
# (리뷰 수 순)로 조회해 인기 장소를 후보 풀에 심고, 응답의 좌표·주소·카테고리를 그대로
# 승계한다(카카오로 다시 공식화하지 않는다).
#   ※ 블로그 검색은 9단계(PlaceSignalStage) 조건부라 V1에서는 호출하지 않는다.
#     초안은 블로그가 주 용처였고 이 섹션도 그 기준으로 쓰였으나, 후보 공급 층이 인기도를
#     시딩으로 앞에서 반영하면서 사후 정렬층이 V1에서 빠졌다.
#
# 검색 API는 developers.naver.com이 아니라 네이버 클라우드 플랫폼(NCP)의 NAVER API HUB로 제공된다.
#   발급: NCP 콘솔 > NAVER API HUB. 등록은 "검색(Search)" 서비스 단위이고 그 아래
#         뉴스·블로그·지역·카페글 등이 함께 제공된다.
#   호출: https://naverapihub.apigw.ntruss.com/search/v1/local   (4-2 실호출로 확정)
#         ★ 레거시 경로(/v1/search/local.json)는 404다. 이관되며 확장자가 사라지고 세그먼트
#           순서가 뒤집혔다. 호스트가 틀려도 같은 404가 나오므로(와일드카드 catch-all) 404만
#           보고 원인을 짐작하지 말 것. 블로그(/search/v1/blog)는 이 키에서 아직 미활성화라
#           401이며, 9단계를 켜려면 NCP 콘솔에서 활성화가 먼저다.
#   인증 헤더: X-NCP-APIGW-API-KEY-ID(=Client ID) / X-NCP-APIGW-API-KEY(=Client Secret)
#             ※ 구 방식의 X-Naver-Client-Id / X-Naver-Client-Secret 이 아니다.
#
# 요금은 0원이다(0~775,000건 구간). 다만 일 최대 25,000건 호출 제한이 있고, 코스 1건 생성에
# 약 18~30회를 호출하므로 하루 약 830~1,400건이 상한이다(10단계 캐싱이 붙으면 약 2배).
# 넘겨도 서비스는 죽지 않는다 — fail-open이라 시드·스타일 후보만 빠지고 코스 생성은 계속된다.
# (docs/tasks/ai-course-create/steps/STEP-0-prerequisites.md 참고)
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=

# --- 한국관광공사 TourAPI (후보 공급 — 관광 슬롯 커버리지·분류) ---
# application.yml의 tour.service-key 가 참조한다(TourApiClient).
#
# ATTRACTION·VIEWPOINT·WALK·ACTIVITY 슬롯의 후보를 공급한다. 카카오 AT4는 관광지에 희소·잡음이고
# 무인지 지역에서 비는 반면, TourAPI는 전국을 행정단위로 균질하게 덮고 cat3 세분류가 스타일
# 태그로 직결된다. 인기도 정렬이 없다는 공백은 위 네이버 시더가 메운다.
#
#   발급: 공공데이터포털(data.go.kr) > "한국관광공사_국문 관광정보 서비스_GW" 활용신청
#         심의유형이 "개발단계: 자동승인"이라 신청 즉시 발급된다. 다만 포털↔한국관광공사
#         동기화에 10~30분이 걸려, 그 사이에는 SERVICE_KEY_IS_NOT_REGISTERED_ERROR 가 난다.
#   호출: https://apis.data.go.kr/B551011/KorService2
#         ※ KorService1 은 2025-08-10 부로 중지됐다.
#   인증: serviceKey 쿼리 파라미터. Encoding/Decoding 두 가지로 발급되는데 어느 쪽을 넣어도 된다
#         — TourApiConfig.normalizeServiceKey 가 Encoding 형태로 통일한다(ROADMAP 4-7).
#
# 무료이고 저장 제약이 없다(구글·TripAdvisor를 결격 처리한 이유가 저장 약관이었다).
# 개발계정 일 1,000건이 그대로 서비스 상한이다 — 요청당 9회 이하이고 격자 캐시로 대부분
# 히트하므로 하루 110코스 이상이다. 운영계정 전환은 이 로드맵의 범위가 아니다.
# 실패해도 fail-open — 그 슬롯은 시더 + 파라메트릭 제안만으로 진행한다.
TOUR_API_KEY=

# --- Redis (인기 코스 캐싱 / 조회수 카운터) ---
# docker-compose.yml에서 호스트 포트를 6479로 매핑한다 (6379는 로컬에 네이티브 Redis가 떠 있을 수 있어 충돌 방지 목적으로 회피).
# 네이티브 Redis로 돌린다면 그 회피가 필요 없으므로 기본 포트인 6379로 바꾼다.
REDIS_HOST=localhost
REDIS_PORT=6479

# --- Grafana Cloud (로컬 Alloy 검증 전용 — 앱은 이 값을 읽지 않는다) ---
# 비워 둬도 앱은 정상 기동한다. 운영 인스턴스는 이 파일이 아니라 SSM Parameter Store의
# /yourtrip/prod/grafana/* 에서 받아가므로(terraform/prod/README.md), 여기 값이 배포로 새지 않는다.
#
# 여기에 두는 이유는 로컬에서 docker로 Alloy를 띄워 검증할 때 --env-file로 넘기기 위해서다.
# 설계와 검증 절차는 docs/tasks/monitoring-config/README.md 참고.
#
# 값은 Grafana Cloud Portal(grafana.com/orgs) → 스택 Details의 각 카드에서 가져온다.
# ⚠️ Prometheus와 Loki의 숫자 ID는 서로 다른 값이다. 같은 값을 넣으면 한쪽이 401로 막히는데,
#    가장 흔한 실수다.
GRAFANA_CLOUD_PROM_URL=
GRAFANA_CLOUD_PROM_USER=
GRAFANA_CLOUD_LOKI_URL=
GRAFANA_CLOUD_LOKI_USER=
# Access Policy 토큰. 스코프는 metrics:write 와 logs:write 둘 다 필요하다 — Alloy가 지표와
# 로그를 같은 토큰 하나로 보내기 때문이다(deploy/prod/config.alloy의 password_file).
# 각 카드의 "Generate now"로 만든 서비스별 토큰은 한쪽 스코프만 가져 맞지 않는다.
GRAFANA_CLOUD_TOKEN=
