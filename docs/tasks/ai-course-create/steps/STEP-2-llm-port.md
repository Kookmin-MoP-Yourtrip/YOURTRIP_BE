# STEP-2. `LlmClient` 벤더 중립 추상화 — 실행 기록

> [ROADMAP.md](../ROADMAP.md) 2단계의 실행 기록이다. 이 단계는 **동작 변화가 없다** — 기존 `MyCourseServiceImpl` → `GeminiService` 경로는 그대로 두고, 새 포트는 8단계 스위치 전까지 프로덕션에서 호출되지 않는다.
>
> 결론부터: **포트와 어댑터는 계획대로 나왔지만, 검증 과정에서 재시도 계층이 셋으로 흩어져 있다는 것이 드러났다.** 설정한 3회가 실제 HTTP 요청 6회로 관측됐고, 그중 하나는 429를 "재시도해도 소용없음"으로 분류하고 있었다. 자세한 것은 판정 1·2.
>
> **[경고] 이 측정의 `results/` 산출물은 소실돼 재현할 수 없다.** 아래 수치는 이 문서에
> 전사된 판정표 82건(luna 40 + nano 42)에만 근거하며, **카카오 검증도 이름 게이트 이전
> 로직으로 측정된 값**이라 현행 기준(프로덕션 `lookupBestPlace()` 호출)과 직접 비교되지
> 않는다. 별도 측정 문서(`AI-HALLUCINATION-OPENAI.md`)는 삭제했고 **이 문서가 유일한
> 기록이다.** 다만 수동 검증의 판정 기준 자체는 현행과 같다 — 아래 "수동 검증 40건"이
> 명시하듯 `NO_RESULT`도 원안 미실존이면 환각으로 판정했다(현행 `FABRICATED` 기준과 동일).
>
> 진행 상황: **2단계 전량 완료.** 테스트 103개 통과, 앱 기동 확인, baseline 재측정 4조합 120요청, 수동 검증 82건(luna 40 + nano 42)까지 끝냈다. **복합 환각률 25.6% → 7.5%**(−18.1%p).
>
> **측정이 예상을 두 군데서 뒤집었다.** ① Curator 후보였던 `gpt-5-nano`는 환각률이 luna의 **7배**라 쓸 수 없다(판정 7) ② 로드맵 목표 1이 세운 인과 "구조화 출력을 켜면 파싱 실패가 사라진다"는 성립하지 않는다 — 실제로는 **모델 교체**가 해결했다(판정 9).

## 설계 결정 — 로드맵에 없던 네 가지

로드맵 2단계는 만들 것만 적혀 있고 "어떻게"는 열려 있었다. 착수 전에 네 가지를 확정했다.

| # | 결정 | 대안을 버린 이유 |
|---|---|---|
| 1 | **측정점을 둘 다 찍는다** (프롬프트지시 판 + json_schema 판, 각각 luna/nano) | 한 점만 찍으면 "모델 교체"와 "구조화 출력" 중 하나가 파이프라인 효과에 섞인다. 아래 "왜 측정점이 둘인가" |
| 2 | **어댑터가 `OpenAiChatModel`을 직접 조립** | auto-config는 API 키를 기동 필수로 만들어 "동작 변화 없음"을 깨고, `baseUrl`을 못 바꿔 WireMock 검증이 불가능해진다 |
| 3 | **절단은 어댑터가 예외로 번역** | 포트 반환 타입을 래퍼로 감싸면 모든 에이전트가 래퍼를 벗겨야 하고, 벤더별로 다른 종료 사유가 포트로 샌다 |
| 4 | **import 스캔 테스트 + 목킹 데모** | ArchUnit은 규칙 하나 때문에 의존성을 늘린다 |

### 왜 측정점이 둘인가

Gemini before 값(당시 복합 지표 25.6%)에서 8단계 후 값까지, 실제로 바뀌는 변수는 셋이다.

```
V1 모델        gemini-2.5-flash  →  OpenAI
V2 출력 강제   프롬프트에 글로    →  response_format.json_schema (디코딩 레벨)
V3 구조        단일 호출          →  5단계 파이프라인
```

중간점을 하나만 찍으면 어느 쪽이든 두 변수가 뭉친다.

| 중간점 | 앞 구간 | 뒤 구간 |
|---|---|---|
| (OpenAI, 프롬프트지시, 단일) | V1 | **V2+V3** |
| (OpenAI, json_schema, 단일) | **V1+V2** | V3 |
| **둘 다** | V1 / V2 / V3 각각 | — |

덤이 하나 붙는다. [BASELINE-ARTIFACT-ANALYSIS.md](../hallucination/BASELINE-ARTIFACT-ANALYSIS.md) 판정 3은 "파싱 실패 5건이 전부 절단이라 **구조화 출력으로는 안 사라진다**"고 결론냈는데, 이건 아직 추론이다. V2를 켠 판과 끈 판을 둘 다 재면 그 주장이 숫자로 확인된다.

모델 축도 붙는다 — 0단계가 `gpt-5.6-luna`의 한국어 지역 지식을 확인하지 못한 채 Curator에 배정했으므로(ROADMAP 0-4), luna/nano를 같은 조건으로 재서 확정한다.

---

## 판정 1 — 재시도가 세 계층에 흩어져 있었다 ★

WireMock으로 429를 계속 돌려주게 두고 `llm.retry.attempts: 3`으로 부르면 **실제 HTTP 요청이 6건** 나갔다.

원인은 우리 `LlmRetryExecutor` 위아래로 재시도가 두 개 더 있었기 때문이다.

| 계층 | 무엇이 | 기본 동작 |
|---|---|---|
| Spring AI `OpenAiChatModel` | 자체 `RetryTemplate` | 다중 시도 + 자체 백오프 |
| **Apache HttpClient 5** | `DefaultHttpRequestRetryStrategy` | **429·503을 1회 더 시도** |
| 우리 코드 | `LlmRetryExecutor` | `llm.retry.attempts` |

Apache가 끼어든 경위가 특히 고약하다. `ClientHttpRequestFactoryBuilder.detect()`가 클래스패스를 보고 HTTP 스택을 고르는데, **httpclient5는 이 레포에 선언조차 되지 않은 전이 의존성**이다(`compileClasspath`에는 없고 `runtimeClasspath`에만 있다). 즉 아무도 의도하지 않았는데 HTTP 계층과 재시도 정책이 그쪽으로 정해져 있었다.

**대응**

- `OpenAiChatModel.retryTemplate`을 `maxAttempts(1)`로 고정
- `ClientHttpRequestFactoryBuilder.reactor()`로 HTTP 스택을 못박음. reactor-netty를 고른 이유는 ① 상태 코드 기반 자동 재시도가 없고 ② 이 레포의 다른 외부 호출(`KakaoConfig`)이 이미 reactor-netty라 스택이 하나로 모이기 때문이다
- JDK HttpClient(`jdk()`)도 후보였으나 WireMock과의 HTTP/2 협상에서 깨져 배제했다

결과적으로 **재시도 정책이 `llm.retry` 한 곳에만 존재한다.** `doesNotDoubleRetry` 테스트가 이 성질을 고정한다 — 요청 수가 정확히 `attempts`와 같은지 센다.

> 이 발견은 `max-concurrent-calls` 초기값(2)의 의미와도 직결된다. 세마포어로 동시 호출을 2로 묶어도 각 호출이 뒤에서 몰래 2배로 시도하고 있었다면, rate limit 방어라는 목적 자체가 성립하지 않는다.

---

## 판정 2 — Spring AI는 429를 "재시도 무의미"로 분류한다 ★

위 6회를 3회로 줄이고 나니 이번엔 재시도가 **아예 안 걸렸다.** 예외를 열어보니 이랬다.

```
org.springframework.ai.retry.NonTransientAiException: 429 -
    at org.springframework.ai.retry.RetryUtils$1.handleError(RetryUtils.java:73)
```

`RetryUtils.DEFAULT_RESPONSE_ERROR_HANDLER`는 **4xx 전체를 `NonTransientAiException`**(다시 시도해도 소용없음), 5xx만 `TransientAiException`으로 나눈다. 429는 4xx라 여기 걸린다 — 이름이 뜻하는 것과 실제가 정반대다.

**대응.** 메시지 문자열에서 `"429"`를 찾는 방법(기존 벤치마크 하네스가 쓰던 방식)은 벤더가 문구를 바꾸면 조용히 깨지므로 택하지 않았다. 대신 **상태 코드를 보존하는 `responseErrorHandler`를 주입해 분류를 직접 소유한다.**

```java
boolean isRetriable() {
    return status == 429 || status == 408 || status >= 500;
}
```

400(스키마 오류)·401(키 오류)은 재시도하지 않는다 — 같은 요청을 다시 보내면 같은 답이 오고, 백오프를 태우면 오류를 늦게 볼 뿐이다.

---

## 판정 3 — 설계 문서의 LLM 포트 설계의 재시도 배치는 그대로 구현할 수 없었다

LLM 포트 설계의 표는 의미 계층 재시도를 `LlmResponseParser`의 책임으로 적었다.

| 계층 | 대상 | 구현 |
|---|---|---|
| 전송 | 429 / 5xx | 어댑터가 `llm.retry` 설정으로 지수 백오프 + 지터 |
| 의미 | 200 OK인데 깨진 JSON | `LlmResponseParser` 실패 시 1회만 재시도 |

그런데 **재시도하려면 LLM을 다시 불러야 하고, 파서는 그걸 할 수 없다.** 프롬프트를 재조립해 다시 보내는 것은 어댑터의 일이다.

**대응 — 책임을 셋으로 쪼갰다.**

- `LlmResponseParser` — 순수 변환만. 외부 의존이 없어 완전히 결정론적으로 테스트된다
- `LlmRetryExecutor` — 전송 계층 백오프·지터. **어댑터에서 떼어낸 이유는 테스트**다. 어댑터 안에 두면 백오프 간격을 WireMock 응답 타이밍으로 간접 확인할 수밖에 없는데, `backoffMillis`를 순수 함수로 두면 값으로 직접 검증된다
- `OpenAiLlmClient` — 둘을 조립하고 의미 재시도를 오케스트레이션

`Sleeper`를 갈아끼워 실제로 자지 않으므로, 백오프가 4초까지 늘어나는 시나리오도 밀리초 안에 끝난다.

---

## 판정 4 — 측정을 위해 프로덕션 쪽을 두 곳 보완해야 했다

2-6 하네스를 쓰다 보니 포트에 빠진 것이 드러났다.

**① 절단된 응답의 원문이 버려지고 있었다.** 어댑터가 `finish_reason`만 보고 예외를 던지면서 받은 텍스트를 흘려버렸다. 그런데 절단은 **출력 상한에 닿은 것**과 **스트림이 끊긴 것**으로 갈리고 대응이 다르다. 판정 3이 "정상 응답은 1,400~1,660바이트인데 이 건은 386바이트"라는 비교로 상한이 원인이 아님을 밝힌 것이 정확히 그 예다. → `LlmTruncatedResponseException`이 `partialText`를 들고 있게 했다.

**② 의미 재시도가 측정을 오염시킨다.** 깨진 응답을 한 번 더 물어 고치면 파싱 실패율이 "재시도 후" 값이 되는데, Gemini 측정값(16.7%)에는 그런 보정이 없었다. → `llm.retry.semantic-attempts`로 빼서 baseline은 1로 내려 잰다. 설계값 2는 프로덕션 기본값으로 유지한다.

---

## 판정 5 — 하네스가 뭉쳐 세던 것을 넷으로 갈랐다

기존 하네스는 호출 실패와 파싱 실패를 `parseSuccess` 한 칸에 뭉쳐 넣고 있었다. 그래서 "파싱 실패율 28.6%"가 **호출이 14건만 성공한 배치 기준**이라는 사실이 몇 달 뒤 재분석에서야 드러났다(전체 30요청 기준은 16.7%).

결말을 넷으로 나눴다.

| 결말 | 의미 | 파싱 실패율의 분자인가 |
|---|---|---|
| `OK` | DTO로 역직렬화 성공 | — |
| `CALL_FAILED` | 재시도 소진(429/5xx/타임아웃) | **아니다.** 응답 자체가 없다 |
| `TRUNCATED` | 응답이 끝까지 오지 않음 | 맞다. 단 구조화 출력으로 막히지 않는다 |
| `PARSE_FAILED` | 응답은 왔는데 스키마 위반 | 맞다. 구조화 출력이 없애야 할 바로 그 실패 |

리포트가 **두 분모를 모두 출력**한다(전체 요청 기준 / 호출 성공분 기준). 로드맵이 요구한 "재측정 시 분모를 반드시 명시한다"를 코드로 강제한 것이다.

`finishReason`과 응답 바이트 수도 함께 기록하고, **성공 응답의 원본까지 남긴다** — 정상 응답의 크기 분포가 있어야 절단된 건이 이상한지 판단할 수 있다.

---

## 비교 가능성을 위해 건드리지 않은 것

2-6이 의미를 가지려면 같은 자로 재야 한다.

- 입력 세트 (지역 10 × 키워드셋 3, 3일 고정)
- `KakaoLocalClient.score()` 리플렉션 호출과 점수 밴드 경계
- 층화 추출 시드 42
- **프롬프트 95줄 원문** — json_schema 판에서도 스키마 구간을 빼지 않는다. 빼면 V2 외에 프롬프트까지 변수가 되어 측정이 무의미해진다(프롬프트 슬림화는 6-6의 일이다)

프롬프트는 복사하지 않고 `GeminiService.buildPrompt()`를 그대로 호출한다. 하네스가 원문을 복사해 가면 drift가 생겨 "모델 교체 효과"를 재지 못한다 — `KakaoConfig.buildKakaoWebClient`가 같은 이유로 만들어진 선례다. **프롬프트 본문은 한 글자도 바뀌지 않았다**(구조만 메서드로 분리).

---

## 이 저장소 최초의 `@ConfigurationProperties`

지금까지 모든 설정이 `@Value` 필드 주입이었다. 여기서 방식을 바꾼 것은 취향이 아니라 **구조 때문**이다.

- `agents.planner.model`처럼 **2단계 중첩 + 키가 열린 맵**은 `@Value`로 표현 자체가 안 된다
- `@Validated`가 모델 ID 오타를 **기동 시점에** 잡는다. 요청 30건을 날리고 나서 아는 것과 부팅이 실패하는 것의 차이다
- `agents`를 `Map<String, Agent>`로 두면 `LlmCall.agentName`이 그대로 조회 키가 된다

바인딩 테스트는 **픽스처가 아니라 배포되는 `application.yml` 원본**을 읽는다. 확인하려는 것이 "record가 바인딩 가능한 모양인가"가 아니라 "우리가 실제로 배포하는 설정이 유효한가"이기 때문이다.

---

## 포트 격리를 테스트로 강제한다

설계 문서의 LLM 포트 설계는 "에이전트 코드가 벤더 SDK 타입을 한 개도 import하지 않는다"를 이 작업의 성과로 내세운다. 그런데 **주장만으로는 지켜지지 않는다** — 6~9단계에서 에이전트를 만들다 보면 `OpenAiChatOptions` 하나만 잠깐 쓰고 싶은 순간이 온다.

- `LlmPortIsolationTest` — `global/ai` 아래에서 `openai/`를 뺀 모든 소스에 벤더 import가 없는지 검사한다. **어댑터가 실제로 벤더 SDK를 쓴다는 것도 함께 단언**해, 검사기가 헛돌아서 통과한 것이 아님을 보인다
- `LlmClientMockingDemoTest` — 6단계 에이전트의 대역을 목으로 검증한다. `com.google.genai.Client`가 `public final`이라 목킹이 불가능했던 것이 포트의 유일한 근거였으므로, **이 테스트가 초록불인 것 자체가 2단계의 성과다**

어댑터도 벤더 예외를 전부 포트 예외로 번역한다. 하나라도 새면 위쪽 파이프라인이 그걸 잡으려고 Spring AI를 import하게 되고, 그 순간 포트가 무의미해진다. 400 응답조차 `LlmTransportException`으로 나오는지 테스트가 확인한다.

---

## 검증 기록

**테스트** — 103개 전부 통과, 실패·스킵 0 (2단계에서 30개 추가).

| 테스트 | 확인하는 것 |
|---|---|
| `AiLlmPropertiesTest` (5) | 배포되는 yml이 검증을 통과해 바인딩되는가, 키 없이도 되는가 |
| `LlmResponseParserTest` (3) | 순수 변환, 실패 시 원문 보존 |
| `LlmRetryExecutorTest` (6) | 백오프·지터 값, 재시도 소진, 비대상 예외 통과 |
| `OpenAiLlmClientTest` (11) | 요청 본문 형태, 실패 번역, 재시도 2계층, 동시 호출 게이트 |
| `LlmPortIsolationTest` (2) | 벤더 SDK 유출 |
| `LlmClientMockingDemoTest` (3) | 포트 목킹 가능성 |

**기동 확인** (로드맵 원칙: 컴파일 성공으로 끝내지 않는다)

```
LLM 어댑터 활성화: provider=openai, timeoutMs=20000, maxConcurrentCalls=2,
  agents=[planner=gpt-5.6-luna(t=0.7), curator=gpt-5.6-luna(t=0.9), place-profile=gpt-5-nano(t=0.2)]
Started YourtripApplication in 13.738 seconds
```

- **`OPENAI_API_KEY` 없이 기동됐다** — 결정 2가 지켜졌다는 증거다. `api-key: ${OPENAI_API_KEY:}`의 빈 기본값과 `spring.ai.model.chat: none` 유지가 그 조건이다
- 어댑터가 조건부 빈이라 기동 성공만으로는 활성 여부가 드러나지 않아, **활성 provider와 agent별 모델을 기동 로그에 남기게 했다**(운영에서도 설정 파일을 열지 않고 확인된다). API 키는 찍지 않는다
- 헬스 UP, 기동 중 ERROR 0건
- AI 코스 생성 경로는 **E2E 검증 대상이 아니다**(로드맵 원칙은 1·8·9단계만 지정). `domain/`과 `global/gemini/`의 변경이 0줄인 것으로 무변경을 확인했다

**기동 중 잡은 결함.** `LlmRetryExecutor`와 `OpenAiLlmClient`가 테스트용 생성자를 하나씩 더 갖고 있는데 주입 대상을 표시하지 않아, Spring이 기본 생성자를 찾다가 컨텍스트가 통째로 깨졌다. 단위 테스트만 돌렸다면 못 봤을 결함이다.

---

## 커밋

| 커밋 | 항목 |
|---|---|
| `dcd52e2` | 2-1 포트 + `LlmCall` + 예외 계층 |
| `5c1b93c` | 2-3 `AiLlmProperties` + 설정 외부화 |
| `4ce0a21` | 2-4 `LlmResponseParser` + 전송 계층 재시도 |
| `e196a8e` | 2-2 `OpenAiLlmClient` 어댑터 (숨은 재시도 2개 제거 포함) |
| `f35a2dc` | 2-5 포트 격리 테스트 |
| `d1cbc60` | 기동 로그 |
| `17d099e` | 절단 원문 보존 + 의미 재시도 설정화 |
| `05f39b1` | 2-6 하네스 교체 |

구현 순서를 로드맵 번호와 다르게 가져갔다 — 어댑터(2-2)가 `AiLlmProperties`(2-3)와 `LlmResponseParser`(2-4)를 생성자로 받으므로 의존 방향대로 2-3 → 2-4 → 2-2 순서로 만들었다.

---

## 판정 6 — 모델이 거부하는 옵션이 둘이었다 ★

측정을 돌리자마자 400이 떨어졌다. **둘 다 설계 문서가 전제하던 것을 뒤집는다.**

### ① 커스텀 `temperature`를 받지 않는다

```
"Unsupported value: 'temperature' does not support 0.3 with this model.
 Only the default (1) value is supported."
```

`gpt-5.6-luna`·`gpt-5-nano` 모두 그렇다. **설계 문서의 LLM 포트 설계의 agent별 온도 차등**(Planner 0.7 / Curator 0.9 / PlaceProfile 0.2)이 근거를 잃는다. 그 근거였던 "장소 선정은 다양해야 하고 판정은 일관돼야 한다"는 상충 요구 중, **Curator 쪽(높은 온도)은 기본값 1이 이미 높아 우연히 충족**되지만 **PlaceProfile 쪽(낮은 온도로 충실성 확보)은 그대로 손해**다. 9단계에서 닫힌 태그 집합과 스키마 강제로 보완해야 한다.

설정 키는 지우지 않고 nullable로 두었다 — 온도를 받는 모델로 바꾸면 값만 채우면 되고, 구조를 지우면 "왜 차등하지 않는가"라는 정보까지 사라진다.

> **곁가지 하나.** 값을 안 보내도 실려 나갔다. `OpenAiChatModel.Builder`가 `defaultOptions`를 **`temperature 0.7`로 초기화**해두기 때문이다(바이트코드로 확인). 호출마다 옵션을 만들어 넘겨도 지정하지 않은 필드는 이 기본값이 병합된다. 빈 `defaultOptions`로 덮어야 "안 보내는 것"이 실제로 안 보내진다.

### ② `gpt-5-nano`는 추론에 예산을 다 쓰고 본문을 못 낸다

```
finishReason=LENGTH, 0바이트 수신
```

`max-output-tokens: 4096`인데 **추론 토큰이 그걸 다 먹었다.** 설계 문서의 비용 분석이 경고한 "추론 토큰이 숨은 변수다"가 그대로 나타난 경우다.

모델과 출력 강제가 동시에 바뀐 상태였으므로 원인을 갈랐다.

| | prompt | json_schema |
|---|---|---|
| **luna** | 정상 (1,473B) | 정상 (605~650B) |
| **nano** | 절단 0B | 절단 0B |

**모델 문제다.** 대응은 `reasoningEffort`를 agent별 설정으로 노출하는 것 — 설계 문서의 LLM 포트 설계이 Gemini 전용 `thinking-budget`을 제거하며 *"OpenAI에 대응하는 추론 강도 설정이 있다면 어댑터 내부에서 다룬다"*고 열어둔 자리가 정확히 여기다. 낮추면 nano가 살아난다(0바이트 → 883바이트, 장소 17개).

**지원값이 모델마다 다르다는 것도 실측으로 알았다.**

| | `none` | `minimal` | `low` |
|---|---|---|---|
| luna | ✅ | ❌ | ✅ |
| nano | ❌ | ✅ | ✅ |

luna는 `minimal`을, nano는 `none`을 거부한다. **두 모델을 같은 조건으로 비교하려면 공통 최저값인 `low` 하나뿐**이다. 이 사실은 측정 축을 정하는 데 그대로 제약이 됐다.

> 이 두 발견은 **WireMock만으로는 절대 나오지 않는다.** 스텁은 우리가 보낸 것을 되돌려줄 뿐 모델이 무엇을 거부하는지 모른다. 30요청을 태우기 전에 2요청짜리 스모크를 먼저 돌린 것이 여기서 값을 했다 — 그리고 `minimal`로 30요청을 시작했을 때는 **연속 3회 실패 조기 중단**이 걸려 3요청만 소모됐다.

---

## 2-6 측정 결과 — 네 조합 전량 완료

> 측정의 해석과 결론은 한때 별도 문서(`AI-HALLUCINATION-OPENAI.md`)로 분리했으나, **원본 산출물이 소실돼 그 문서는 삭제했다.** 이 절과 아래 판정 7~10, 수동 검증 판정 전량이 이 측정의 유일한 기록이다. before 측정은 [AI-HALLUCINATION-GEMINI.md](../hallucination/AI-HALLUCINATION-GEMINI.md).

**공통 조건**: 추론 `low` / 의미 재시도 1회 / `max-output-tokens` 4096 / 온도 미전송(모델이 거부)
**규모**: 4조합 × 30요청 = **120요청**, 총 약 32분
**산출물**: `results/*-openai-{model}-{mode}-relow-*` (조합별 원본 응답 30건 + CSV 3종 + 수동 검증 표본 40건)

| 조합 | 장소 | **자동 프록시 환각률** | JSON 실패(전체 요청) | 정상 응답 크기(평균) |
|---|---|---|---|---|
| **luna / 프롬프트지시** | 454 | **6.4%** | 0.0% | 1,503B |
| luna / json_schema | 473 | 7.8% | 0.0% | 786B |
| nano / 프롬프트지시 | 448 | **47.8%** | 3.3% (1/30) | 1,553B |
| nano / json_schema | 454 | **50.7%** | 0.0% | 798B |

점수 밴드 분포(%)를 보면 차이가 어디서 오는지 바로 드러난다.

| 조합 | `NO_RESULT` | `S0` | `S1_4` | `S5_7` | `S8_10` |
|---|---|---|---|---|---|
| luna / 프롬프트지시 | 2.2 | 0.0 | 4.2 | 9.0 | **84.6** |
| luna / json_schema | 3.2 | 0.0 | 4.7 | 7.2 | **85.0** |
| nano / 프롬프트지시 | **40.8** | 0.4 | 6.5 | 18.1 | 34.2 |
| nano / json_schema | **44.3** | 0.7 | 5.7 | 13.7 | 35.7 |

### 판정 7 — Curator 모델은 `gpt-5.6-luna`로 확정한다 ★

0단계는 `gpt-5.6-luna`의 한국어 지역 지식을 확인하지 못한 채 배정하고 **"2-6 실측으로 최종 확정"**을 숙제로 넘겼다(ROADMAP 0-4). 답이 나왔다 — **nano는 luna보다 환각률이 압도적으로 높다** — 자동 프록시 47.8% vs 6.4%(7.5배)이고, 수동 검증까지 반영한 **복합 지표로는 89.4% vs 7.5%(12배)**, 매칭 실패를 걷어낸 **진짜 환각률로는 41.7% vs 1.08%(38.6배)** 다. 해석과 결론도 이 문서의 아래 절들에 있다.

nano 응답의 `NO_RESULT`가 40%를 넘는다는 것은 **AI가 부른 이름의 40%가 카카오에서 검색조차 안 된다**는 뜻이다. 실제로 뽑아보면 전형적인 환각이다.

```
경주 전통찜닭골목 / 교동시장 센터점 / 경주 남시장 / 불국사 석가탑 방향 산책로
부산항대교 남항스카이워크 / 영도대교 전망공원 / 광복동 카페 거리
```

그럴듯한 조어지만 존재하지 않는다. **비용을 아끼려고 Curator를 nano로 내리는 선택은 이 작업의 1차 목표(환각률)를 정면으로 훼손한다.**

> **PlaceProfile의 nano 배정은 이 측정으로 부정되지 않는다.** nano가 실패한 것은 *지식 회상*(한국 지역 상호명을 기억해내기)인데, PlaceProfile이 하는 일은 *주어진 블로그 텍스트에서 속성 추출*이라 과제 성격이 다르다. 다만 **경고로는 남겨둔다** — 9단계에서 닫힌 태그 집합 준수율을 별도로 확인해야 한다.

### 판정 8 — 구조화 출력은 환각률을 낮추지 않는다 (낮출 수도 없다)

| | 프롬프트지시 | json_schema | 차이 |
|---|---|---|---|
| luna | 6.4% | 7.8% | +1.4%p |
| nano | 47.8% | 50.7% | +2.9%p |

**둘 다 오히려 소폭 올랐다.** 표본 오차 범위로 보이지만, 방향이 어느 쪽이든 결론은 같다 — **스키마는 형식을 강제하지 내용을 강제하지 않는다.** "존재하는 상호명을 대라"는 스키마로 표현할 수 없는 요구다. 환각을 없애는 것은 3~5단계의 카카오 그라운딩이지 구조화 출력이 아니며, 이 측정은 그 역할 분담을 데이터로 확인해준다.

**그럼에도 구조화 출력을 쓸 이유는 둘 다 실측으로 확인됐다.**

1. **출력 토큰이 약 절반으로 준다** — 1,503B → 786B(−48%). 원인은 pretty-print 제거다. 프롬프트지시 판은 들여쓰기된 JSON을, json_schema 판은 compact JSON을 돌려준다. 장소 수는 오히려 늘었는데(15.1 → 15.8개/요청) 바이트는 절반이다. 설계 문서의 비용 분석이 *"금액을 지배하는 것은 Curator의 출력(62%)"*이라 했으므로 이건 비용에 직결된다
2. **스키마 밖 필드를 차단한다** — 120요청 중 유일한 파싱 실패가 이것이었다

   ```
   Unrecognized field "placeLocation" (2 known properties: "placeName", "startTime")
   ```

   **프롬프트 내부 불일치가 원인이다.** 프롬프트 요구사항 5번이 `"placeLocation"은 최대한 구체적으로 작성해 주세요`라고 지시하는데 정작 스키마 예시에는 그 필드가 없다. luna는 무시했고 nano는 곧이곧대로 따랐다. **`additionalProperties: false`가 이걸 원천 차단**해 json_schema 판에서는 0건이 됐다. (프롬프트 자체의 이 결함은 6-6에서 정리된다)

### 판정 9 — Gemini의 JSON 실패 16.7%는 모델 성질이었다

**120요청 전체에서 절단(`TRUNCATED`)이 0건이다.**

`BASELINE-ARTIFACT-ANALYSIS.md` 판정 3은 Gemini의 파싱 실패 5건이 전부 응답 절단이며 *"구조화 출력만으로는 사라지지 않는다"*고 결론냈는데, **그 추론이 맞았다는 것이 반대 방향에서 확인됐다** — 구조화 출력이 아니라 **모델을 바꾸니** 절단이 사라졌다.

즉 로드맵 목표 1이 세운 인과("구조화 출력을 켜면 파싱 실패가 사라진다")는 **성립하지 않는다.** 실제로는 ① 절단은 모델 교체가 해결했고 ② 구조화 출력이 없앤 것은 스키마 밖 필드 1건뿐이다. 결과적으로 목표는 달성됐지만 **달성한 수단이 예상과 다르다.**

### Gemini와의 대조 (3점 비교의 중간점 = luna / 프롬프트지시)

| 지표 | Gemini 2.5-flash | OpenAI luna | |
|---|---|---|---|
| **자동 프록시 환각률**(매칭 실패율) | 19.8% | **6.4%** (29/454) | −13.4%p |
| **JSON 실패율** (전체 요청 기준) | 16.7% (5/30) | **0.0%** (0/30) | −16.7%p |
| 일수 불일치(≠3) | — | 0 / 30 | |
| day당 장소 수 3~6 위반 | — | 0 / 90 day | |
| `startTime` 오름차순 위반 | — | 0건 | |
| 중복 장소 | — | 7건 (1.5%) | |

### 점수 밴드 분포

| 밴드 | Gemini | OpenAI luna |
|---|---|---|
| `NO_RESULT` | 11.6% | **2.2%** |
| `S0` | — | 0.0% |
| `S1_4` | 8.2% | 4.2% |
| `S5_7` | 14.1% | 9.0% |
| `S8_10` | 66.1% | **84.6%** |

지역 tier별로는 FAMOUS 5.1% / MINOR 7.8%로, **덜 알려진 지역이 여전히 더 나쁘다**는 경향은 유지된다(Gemini에서도 같았다).

### 수동 검증 40건 — 복합 지표 완성

밴드별 층화 추출(밴드당 10건, 시드 42) 표본을 하나씩 확인했다. 판정 규칙은 **원 측정과 동일하게 맞췄다** — `NO_RESULT` 밴드도 *"AI 원안이 실존하지 않으면 환각"*으로 본다. 당시 라벨은 `LAUNDERED`였으나 이 기준은 현행 `FABRICATED` 정의(매칭 성공 여부 무관)와 같다([BASELINE-ARTIFACT-ANALYSIS.md](../hallucination/BASELINE-ARTIFACT-ANALYSIS.md) 판정 5).

| 밴드 | 표본 | CORRECT | FABRICATED | WRONG_MATCH | UNVERIFIABLE | 전체 비중 |
|---|---|---|---|---|---|---|
| `NO_RESULT` | 10 | 3 | **3** | 0 | 4 | 2.2% |
| `S1_4` | 10 | 9 | **1** | 0 | 0 | 4.2% |
| `S5_7` | 10 | 7 | 0 | 2 | 1 | 9.0% |
| `S8_10` | 10 | 9 | 0 | 1 | 0 | 84.6% |

```
지어냄률(당시 명칭: 세탁된 환각률)
              = Σ (밴드별 FABRICATED 비율 × 밴드별 전체 비중)
              = 1.08%p  (UNVERIFIABLE = 정답, 전제 ②)

복합 환각률   = 당시 자동 프록시 6.4% + 1.08% = 7.5%
              ※ 당시 프록시는 NO_RESULT+S0+S1_4 기준 — 현행(NO_RESULT만)과 다르다
```

| 측정점 (당시 지표 체계) | 자동 프록시 | 세탁된 환각 | **복합 지표** |
|---|---|---|---|
| Gemini 단일 호출 | 19.8% | 5.7% | **25.6%** |
| **OpenAI luna 단일 호출** | 6.4% | 1.08% | **7.5%** |

**25.6% → 7.5%, −18.1%p (71% 감소).** `UNVERIFIABLE`을 전부 환각으로 보는 상한도 **9.3%**로, before 값의 절반에 못 미친다.
(위 값은 당시 지표 체계 기준이다 — 현행 1차 지표로는 "세탁된 환각" 열이 지어냄률에 해당하며, 5.7%는 이후 재판정·재채점으로 10.1%가 됐다. luna 쪽은 산출물이 소실돼 재채점할 수 없다.)

#### 판정 40건 전량

`results/`는 `.gitignore` 대상이라 CSV 자체는 레포에 남지 않는다. **판정과 근거를 검증 가능하게 남기려면 문서에 옮겨야 한다** — Gemini baseline 산출물이 다른 워크트리에만 남아 몇 달 뒤에야 재분석된 전례가 정확히 이것이다.

<details>
<summary>40건 펼쳐보기</summary>

**`NO_RESULT` — 검색 결과 0건 (비중 2.2%)**

| 지역 | AI 원안 | 판정 | 근거 |
|---|---|---|---|
| 통영 | 봉평동카페거리 | `UNVERIFIABLE` | 봉평동은 통영 실존 행정동이나 '카페거리' 형성 여부 확인 불가 |
| 통영 | 연대도마을식당 | `UNVERIFIABLE` | 연대도 맛집 목록에 동명 업소 미확인. 섬 소규모 식당은 온라인 흔적이 적어 단정 불가 |
| 강릉 | 세인트존스호텔 오션뷰 파인 다이닝 | `UNVERIFIABLE` | 세인트존스호텔(강릉)은 실존하나 이 이름의 업소는 아님 — 서술적 수식 |
| 제주 | 민트 레스토랑 | `CORRECT` | 실존. 휘닉스 아일랜드 글라스하우스 내 파인다이닝, 미쉐린 가이드 선정. 카카오만 미검색 |
| 통영 | 분소식당 | `CORRECT` | 실존. 통영해안로, 졸복국 전문. 카카오만 미검색 |
| 삼척 | 덕항산장 | `FABRICATED` | 덕항산(삼척)은 실존하나 산장 시설은 없다 — 미실존으로 판정 |
| 삼척 | 삼척해수욕장 카페거리 | `UNVERIFIABLE` | 삼척해수욕장은 실존, '카페거리'는 공식 명칭이 아님 |
| 통영 | 연대도몽돌해변 | `CORRECT` | 실존. 통영 연대도에 몽돌해변 2곳. 카카오만 미검색 |
| 공주 | 춘마곡 | `FABRICATED` | 장소명이 아님 — '춘마곡추갑사(봄 마곡사, 가을 갑사)' 관용구를 지명으로 착각 |
| 부산 | 송도삼정횟집 | `FABRICATED` | 부산 송도에 '삼영횟집'은 실존하나 '삼정횟집'은 미확인 — 유사 상호 혼동으로 판단 |

**`S1_4` (3점) — 이름 불일치, 주소만 일치 (비중 4.2%)**

| 지역 | AI 원안 | 카카오 매칭 | 판정 | 근거 |
|---|---|---|---|---|
| 제주 | 한담해안산책로 | 한담마을 장한철산책로 | `CORRECT` | 실존. 매칭된 '한담마을 장한철산책로'가 이 산책로의 공식 명칭 |
| 공주 | 정지산유적 | 정지산 유적 | `CORRECT` | 실존. 띄어쓰기만 다름 |
| 영주 | 영주근대역사문화거리 | 영주 근대역사문화거리 | `CORRECT` | 실존. 띄어쓰기만 다름 |
| 통영 | 강구안문화마당 | 강구안 문화마당 | `CORRECT` | 실존. 띄어쓰기만 다름 |
| 순천 | 순천 드라마촬영장 | 순천드라마촬영장 | `CORRECT` | 실존. 띄어쓰기만 다름 |
| 삼척 | 장호항어촌체험마을 | 삼척장호어촌체험마을 | `CORRECT` | 실존. 공식명 '삼척장호어촌체험마을' |
| 경주 | 경주국립박물관 | 국립경주박물관 | `CORRECT` | 실존. 공식명은 '국립경주박물관'으로 어순만 다름 |
| 서울 | STAY | 신라스테이 광화문 | `FABRICATED` | 장소명이 아님. 잠실 코스(롯데월드·서울스카이) 끝에 나왔는데 종로구 신라스테이 광화문으로 매칭 |
| 공주 | 고마나루 솔밭 | 금강6경(고마나루솔밭) | `CORRECT` | 실존. 매칭된 '금강6경(고마나루솔밭)'이 같은 곳 |
| 경주 | 경주 오릉 | 경주오릉 | `CORRECT` | 실존. 띄어쓰기만 다름 |

**`S5_7` (5점) — 이름 불일치, 주소+카테고리로 도달 (비중 9.0%)**

| 지역 | AI 원안 | 카카오 매칭 | 판정 | 근거 |
|---|---|---|---|---|
| 서울 | 남산서울타워 | N서울타워 | `CORRECT` | 실존. 공식명 'N서울타워'의 통칭 |
| 부산 | 개미집 해운대점 | 개미집 국제시장본점직영점 | `CORRECT` | 실존. 매칭 주소가 해운대구 구남로라 해운대 지점이 맞음(상호 표기만 체인명) |
| 공주 | 공주 메타세쿼이아길 | 공주메타세콰이어숲길 | `CORRECT` | 실존. 공식명 '공주메타세콰이어숲길' |
| 영주 | 영주전통묵집 | 순흥전통묵집 | `CORRECT` | 영주에 전통묵집 실존(순흥전통묵집·전통묵집식당). 상호는 부정확하나 동일 지역·업종의 실제 업소로 매칭 |
| 제주 | 산지천마당 | 올레길 18코스(제주원도심-조천 올레) | `UNVERIFIABLE` | 산지천(제주시 건입동)은 실존하고 올레 18코스 시작점이나 '산지천마당' 공식 명칭은 미확인 |
| 서울 | 종로3가 포장마차거리 | 화신맛의거리 | `WRONG_MATCH` | 실존(한국관광공사 등재). 카카오가 인사동3길 '화신맛의거리'를 반환 |
| 공주 | 금강자연휴양림 | 금강자연휴양림 황토메타길 | `CORRECT` | 실존(세종 금남면). 매칭도 해당 휴양림의 산책로 — 다만 공주 코스에 넣은 것은 별개 문제 |
| 통영 | 통영해물가 | 통영칼국수128 | `WRONG_MATCH` | 실존. 주소(통영해안로 377)가 매칭 결과와 일치하는데 카카오가 '통영칼국수128' 상호를 반환 |
| 공주 | 공주 메타세쿼이아길 | 공주메타세콰이어숲길 | `CORRECT` | 실존. 위 #22와 동일 장소 |
| 제주 | 월정리해변 | 월정리해수욕장 | `CORRECT` | 실존. 공식명 '월정리해수욕장' |

**`S8_10` (8·10점) — 이름 일치(+5) 포함 (비중 84.6%)**

| 지역 | AI 원안 | 카카오 매칭 | 판정 | 근거 |
|---|---|---|---|---|
| 순천 | 대원식당 | 대원식당 | `CORRECT` | 실존. 순천 국밥집 |
| 삼척 | 대금굴 | 대금굴 | `CORRECT` | 실존. 삼척 신기면 |
| 순천 | 선암사 | 선암사 | `CORRECT` | 실존. 순천 승주읍 사찰 |
| 순천 | 흥덕식당 | 흥덕식당 | `CORRECT` | 실존. 순천 역전 식당 |
| 통영 | 통영대풍관 | 대풍관 | `CORRECT` | 실존. 통영 다찌집 '대풍관' |
| 삼척 | 환선굴 | 환선굴휴게실가든 | `WRONG_MATCH` | 환선굴은 실존(삼척 신기면 대이리). 카카오가 미로면 소재 '환선굴휴게실가든'(식당)을 반환 — 이름 부분일치로 +5가 붙은 거짓 양성 |
| 제주 | 아쿠아플라넷 제주 | 아쿠아플라넷 제주 | `CORRECT` | 실존. 서귀포 성산읍 |
| 부산 | 송정집 | 송정집 | `CORRECT` | 실존. 부산 해운대 송정 |
| 부산 | 전포카페거리 | 전포카페거리 | `CORRECT` | 실존. 부산진구 |
| 경주 | 보문호 | 보문호 | `CORRECT` | 실존. 경주 보문관광단지 |

</details>

`nano / 프롬프트지시` 조합도 같은 방식으로 42건을 판정했다(`S0` 밴드가 2건 있어 40이 아니라 42다).

<details>
<summary>nano 42건 펼쳐보기</summary>

**`NO_RESULT` — 검색 결과 0건 (비중 40.8%)**

| 지역 | AI 원안 | 판정 | 근거 |
|---|---|---|---|
| 서울 | 더플레이스 홍대점 | `CORRECT` | 실존. CJ푸드빌 이탈리안 비스트로 '더플레이스' L7홍대점(21층). 상호는 '홍대 L7점' |
| 공주 | 삼한본색 | `FABRICATED` | 공주 맛집 목록·시 관광 사이트 어디에도 없다 — 미실존으로 판정 |
| 영주 | 영주관광문화센터 | `FABRICATED` | 영주에 이 이름의 시설이 없다. 유사한 영주문화관광재단은 관광지가 아닌 기관 — 어순을 뒤집은 조어로 판정 |
| 공주 | 공주 성왕루 | `FABRICATED` | 공산성 누각은 공북루·만하루·진남루·쌍수정이며 '성왕루'는 없다. 백제 성왕에서 딴 조어 |
| 통영 | 통영김밥촌 식당 | `FABRICATED` | 충무김밥 거리는 실존하나 이 상호는 없다 — 거리 이름에 식당을 붙인 조어로 판정 |
| 삼척 | 명물횟집 해물코스 | `FABRICATED` | '해물코스'는 메뉴명이라 상호가 될 수 없다 — 업소명과 메뉴를 붙인 조어 |
| 영주 | 카페 바람난책방 | `FABRICATED` | 영주 카페 목록에 없다(유사 북카페는 구구리책다방) — 미실존으로 판정 |
| 서울 | 가로수길 카페 베네 | `UNVERIFIABLE` | 카페베네 체인은 실존하나 가로수길 지점은 미확인 |
| 삼척 | 오대의리 맛집 삼척점 | `FABRICATED` | '맛집'이 상호에 포함된 조어. '~삼척점'이라는 체인 표기도 근거 없음 |
| 부산 | 카페 드 바다 | `FABRICATED` | 부산 오션뷰 카페 목록에 없다 — 미실존으로 판정 |

**`S0` (0점) — 이름·주소·카테고리 전부 불일치 (비중 0.4%)**

| 지역 | AI 원안 | 판정 | 근거 |
|---|---|---|---|
| 통영 | 장사도 등대 | `FABRICATED` | 장사도는 실존하나 등대 시설은 없다. 매칭도 거제 대포등대로 무관 — 미실존으로 판정 |
| 순천 | 낙안정 | `FABRICATED` | 순천 낙안읍성은 실존하나 낙안정이라는 정자는 없다. 매칭도 보성 남극정으로 무관 — 미실존으로 판정 |

**`S1_4` (3점) — 이름 불일치, 주소만 일치 (비중 6.5%)**

| 지역 | AI 원안 | 카카오 매칭 | 판정 | 근거 |
|---|---|---|---|---|
| 통영 | 통영해양과학관 | 경상국립대학교 해양과학대학 해양과학관 | `CORRECT` | 실존. 경상국립대 해양과학대학 해양과학관(통영해안로 2)의 통칭 |
| 경주 | 동궁과 월지 | 동궁과월지 | `CORRECT` | 실존. 띄어쓰기만 다름 |
| 공주 | 공주국립박물관 | 국립공주박물관 | `CORRECT` | 실존. 공식명 '국립공주박물관'으로 어순만 다름 |
| 영주 | 영주댐 주변 산책로 | 용마루2공원 | `UNVERIFIABLE` | 영주댐은 실존하나 '주변 산책로'는 서술적 표현. 매칭은 인근 용마루2공원 |
| 경주 | 교동시장 | 황남시장 | `FABRICATED` | 경주 교동(교촌마을)은 실존하나 교동시장은 없다. 매칭은 다른 시장(황남시장) — 미실존으로 판정 |
| 순천 | 순천박물관 | 순천세계수석박물관 | `FABRICATED` | 순천에 이 이름의 박물관이 없다. 매칭은 순천세계수석박물관으로 다른 시설 — 미실존으로 판정 |
| 통영 | 통영 중앙시장 | 통영중앙전통시장 | `CORRECT` | 실존. 공식명 '통영중앙전통시장' |
| 제주 | 제주도민속자연사박물관 | 제주특별자치도 민속자연사박물관 | `CORRECT` | 실존. 공식명 '제주특별자치도 민속자연사박물관' |
| 순천 | 순천만생태관 | 순천만자연생태관 | `CORRECT` | 실존. 공식명 '순천만자연생태관' |
| 순천 | 순천만 생태관 | 순천만자연생태관 | `CORRECT` | 실존. 위와 동일 시설 |

**`S5_7` (5점) — 이름 불일치, 주소+카테고리로 도달 (비중 18.1%)**

| 지역 | AI 원안 | 카카오 매칭 | 판정 | 근거 |
|---|---|---|---|---|
| 서울 | 명동교자 본점 | 명동교자 명동역점 신관 본점 | `CORRECT` | 실존. 명동교자 본점 |
| 강릉 | 맛집 강릉횟집 | 강릉맛집 피쉬맨횟집 경포대본점 | `FABRICATED` | 상호가 아니라 '맛집 + 지역 + 업종' 조어. 매칭된 '피쉬맨횟집'과 무관 |
| 삼척 | 삼척항 맛집 | 바다횟집 | `FABRICATED` | 상호가 아님 — '지역 + 맛집' 서술. 매칭된 '바다횟집'과 무관 |
| 제주 | 오설록 티 뮤지엄 | 오설록 티뮤지엄 | `CORRECT` | 실존. 띄어쓰기만 다름 |
| 강릉 | 초당두부 본점 | 강릉초당두부 정동진본점 | `UNVERIFIABLE` | 강릉 초당순두부마을은 실존하나 '초당두부 본점'이라는 상호는 미확인 |
| 제주 | 협재 해수욕장 | 협재해수욕장 | `CORRECT` | 실존. 띄어쓰기만 다름 |
| 제주 | 흑돼지거리 맛집 | 탑부평 흑돼지거리본점 | `FABRICATED` | 상호가 아님 — 제주 흑돼지거리는 실존하나 '맛집'은 서술어 |
| 강릉 | 안목해변 카페거리 | 강릉카페거리 | `CORRECT` | 실존. 강릉 커피거리(안목해변)의 통칭, 매칭도 '강릉카페거리'로 같은 곳 |
| 서울 | 삼청동길 카페 | 창신단길 카페 | `FABRICATED` | 상호가 아님. 매칭도 종로구 창신길로 삼청동과 무관 |
| 제주 | 제주도민속촌 | 제주민속촌 | `CORRECT` | 실존. 공식명 '제주민속촌' |

**`S8_10` (8·10점) — 이름 일치(+5) 포함 (비중 34.2%)**

| 지역 | AI 원안 | 카카오 매칭 | 판정 | 근거 |
|---|---|---|---|---|
| 제주 | 성산일출봉 | 성산바다풍경 제주성산일출봉점 | `WRONG_MATCH` | 성산일출봉은 실존. 카카오가 '성산바다풍경 제주성산일출봉점'(업소)을 반환 — 이름 부분일치 거짓 양성 |
| 제주 | 한라수목원 | 한라수목원 | `CORRECT` | 실존 |
| 순천 | 순천만국가정원 | 순천만국가정원 | `CORRECT` | 실존 |
| 순천 | 순천 송광사 | 송광사 | `CORRECT` | 실존 |
| 부산 | 국제시장 | 국제시장 | `CORRECT` | 실존 |
| 삼척 | 대금굴 | 대금굴 | `CORRECT` | 실존 |
| 서울 | N서울타워 | N서울타워 | `CORRECT` | 실존 |
| 부산 | 부산타워 | 부산타워 | `CORRECT` | 실존 |
| 부산 | 해운대해수욕장 | 해운대해수욕장 | `CORRECT` | 실존 |
| 강릉 | 주문진수산시장 | 주문진수산시장 | `CORRECT` | 실존 |

</details>


#### 판정 10 — `S8_10`에서 세탁이 일어나기 어려운 것은 구조 때문이다

표본이 밴드당 10건뿐이라 그것만으로는 근거가 얇다. 그런데 **`score()`의 점수 조합을 보면 구조적 논거가 나온다.**

가점은 이름 `+5` / 주소 `+3` / 카테고리 `+2` 셋뿐이므로 가능한 점수는 `0·2·3·5·7·8·10`이고, **8점 이상은 반드시 이름 일치(+5)를 포함한다**(`8 = 5+3`, `10 = 5+3+2`). 즉 `S8_10`은 **카카오 결과명과 AI 원안이 부분 문자열로 겹치면서 지역까지 맞는** 경우다. 지어낸 이름이 이 둘을 동시에 만족하기는 어렵다.

대신 이 밴드에는 **다른 종류의 거짓 양성**이 있다 — 짧은 이름이 긴 상호에 포함돼 버리는 경우다. 표본에서 나온 `환선굴 → 환선굴휴게실가든`(동굴이 아니라 미로면 소재 식당)이 정확히 그것이고, 이건 `WRONG_MATCH`라 환각에 포함되지 않는다(전제 ③).

반대로 **`S5_7`이 위험 밴드라는 1-2의 진단은 여기서도 유효**하다. `5 = 3+2`(주소+카테고리)로 **이름이 하나도 안 맞아도 5점이 된다.** 표본의 `S5_7` 10건 중 이름이 일치한 건 사실상 없었고, `WRONG_MATCH` 2건이 전부 이 패턴이었다. 다만 **`FABRICATED`는 0건**이다 — luna가 부른 이름이 전부 실존했기 때문이다.

#### 검증 중에 드러난 것 — "카카오가 못 찾았다"와 "존재하지 않는다"는 다르다

`NO_RESULT` 10건 중 **3건이 실존 업소**였다.

| AI 원안 | 확인 결과 |
|---|---|
| 통영 **분소식당** | 실존. 통영해안로, 졸복국 전문 |
| 제주 **민트 레스토랑** | 실존. 휘닉스 아일랜드 글라스하우스, 미쉐린 가이드 선정 |
| 통영 **연대도몽돌해변** | 실존. 연대도에 몽돌해변 2곳 |

BASELINE 문서가 한계로 명시한 *"카카오에 미등록된 실존 업소는 원리적으로 환각과 구분할 수 없다"*가 실제로 관측된 것이다. **자동 프록시 6.4%는 이만큼 과대평가돼 있다.**

`S5_7`의 `WRONG_MATCH` 2건도 같은 성격이다 — `통영해물가`는 **주소(통영해안로 377)까지 매칭 결과와 일치**하는데 카카오가 상호만 다른 `통영칼국수128`을 반환했고, `종로3가 포장마차거리`는 한국관광공사에 등재된 관광지인데 엉뚱한 `화신맛의거리`가 나왔다.

실제로 지어낸 것으로 판정한 3건은 성격이 뚜렷하다.

| AI 원안 | 왜 환각인가 |
|---|---|
| 공주 **춘마곡** | 장소명이 아니다 — `춘마곡추갑사`(봄 마곡사, 가을 갑사) 관용구를 지명으로 착각 |
| 서울 **STAY** | 장소명이 아니다. 잠실 코스 끝에 나왔는데 종로구 신라스테이 광화문으로 매칭 |
| 부산 **송도삼정횟집** | 송도에 `삼영횟집`은 실존하나 `삼정횟집`은 미확인 — 유사 상호 혼동 |

#### 이 값의 한계

- **표본이 밴드당 10건이다.** 원 측정은 밴드당 30건이었으므로(3배치 병합) **신뢰구간이 그때보다 넓다.** 특히 비중 84.6%인 `S8_10`에서 `0/10`은 Wilson 95% 상한이 약 21%라, 산술적으로는 세탁률이 크게 흔들릴 수 있다. 위 판정 10의 구조적 논거가 그 폭을 좁혀주지만 표본이 대신해주지는 않는다
- **판정자는 사람이 아니라 이 세션이다.** 웹 검색으로 확인했고 근거를 `note` 칸에 남겼지만, 소규모 업소는 온라인 흔적이 적어 `UNVERIFIABLE`이 5건 나왔다(전부 `NO_RESULT` 밴드)
- 채운 워크시트는 `results/manual-verification-openai-luna-prompt-relow-*-filled.csv`에 있다(`results/`는 `.gitignore` 대상이라 레포에는 남지 않는다)

### 읽을 때 주의할 것

- **이건 복합 지표 25.6%와 직접 비교하는 값이 아니다.** 25.6% = 자동 프록시 19.8% + 세탁된 환각 5.7%이고, 뒤쪽은 수동 검증이 있어야 나온다. **비교 가능한 짝은 19.8% ↔ 6.4%**다(둘 다 옛 프록시 기준). 수동 검증 40건은 이후 이 문서의 판정표로 전사돼 위 세탁 1.08%까지 완성됐다
- **JSON 실패율 0%가 구조화 출력의 성과는 아니다.** 이 조합은 **스키마를 안 보낸** 판이다. 즉 Gemini의 16.7%는 구조화 출력이 없어서가 아니라 **모델 성질**이었다는 뜻이고, 이는 `BASELINE-ARTIFACT-ANALYSIS.md` 판정 3("원인은 절단이라 구조화 출력으로는 안 사라진다")과 방향이 일치한다 — 절단이 원인이었고, 이 모델은 절단되지 않았다
- **"모델 교체" 축에 딸려온 변수가 둘 있다.** ① 온도(Gemini 0.3 → OpenAI 기본 1) ② 추론 강도(Gemini thinking 기본 활성 → OpenAI `low`). 둘 다 우리가 고를 수 있는 변수가 아니라 **모델 선택에 딸려오는 성질**이다. 특히 온도가 높을수록 환각이 느는 경향을 감안하면 이 측정은 OpenAI에 불리한 조건이므로, 6.4%는 보수적인 값이다
- 응답 크기 1,228~1,764바이트(평균 1,503)로 Gemini 정상 응답(1,401~1,658)과 같은 대역이다. 절단 0건

---

## 남은 작업

측정 자체는 끝났다. 남은 것은 사람 손이 필요한 하나와, 다음 단계로 넘길 관찰들이다.

수동 검증까지 끝나 **2단계는 완료됐다.** 아래는 다음 단계로 넘기는 관찰이다.

**표본을 늘리고 싶다면** — 현재 밴드당 10건은 원 측정(밴드당 30건)보다 적어 신뢰구간이 넓다. `AiHallucinationBaselineTest.SAMPLES_PER_BAND`를 올리면 워크시트가 커진다. 다만 비중 84.6%인 `S8_10`은 위 판정 10의 구조적 논거가 있어, 표본을 늘려 얻는 실익은 `S5_7`(위험 밴드) 쪽이 크다.

- **프롬프트의 `placeLocation` 불일치** — 요구사항 5번이 응답 스키마에 없는 필드를 지시한다. nano가 이걸 따라 파싱 실패를 냈다. 6-6(프롬프트 정리)에서 제거 대상
- **PlaceProfile의 nano 배정 재확인** — 이번 측정이 부정하지는 않지만(과제 성격이 다르다) 9단계에서 닫힌 태그 집합 준수율을 별도로 봐야 한다
- **OpenAI RPM/TPM 티어** — 120요청에서 **429가 0건**이었다. 다만 요청 간 5초 지연 · 동시 호출 1이라 느슨한 조건이었으므로 `max-concurrent-calls: 2`의 근거로는 부족하다. 5단계 병렬화 이후 실측이 맞다
- **`llm.retry.attempts`가 한 번도 발동하지 않았다** — 전송 재시도 경로는 WireMock으로만 검증된 상태로 남는다

## 참고 문서

- [ROADMAP.md](../ROADMAP.md) — 2단계 체크리스트
- [LLM 포트 설계 LLM 연동](../design/LLM-연동.md) — 포트 설계의 근거 ([멀티 에이전트 파이프라인 설계](../멀티-에이전트-파이프라인.md) 허브)
- [BASELINE-ARTIFACT-ANALYSIS.md](../hallucination/BASELINE-ARTIFACT-ANALYSIS.md) 판정 3·4 — 절단 원인과 환각률 정의
- [STEP-0-prerequisites.md](STEP-0-prerequisites.md) — Spring AI 검증, 모델 배치
- [STEP-1-existing-defects.md](STEP-1-existing-defects.md) — 1단계 실행 기록
