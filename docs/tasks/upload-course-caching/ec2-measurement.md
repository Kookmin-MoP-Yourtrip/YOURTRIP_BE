# EC2 분리 환경 실측 — 인기 코스 · 상세 조회 (P1 · P3 · P5 · D2 · D3)

> 캐싱이 실제로 무엇을 해결했는지를 **세 상태를 한 표에 놓고** 확인한 기록이다. [Phase 0 로컬 게이트](phase0-local-gate.md)에서 토글이 의도대로 동작함을 확인한 뒤 진행했다.
>
> **결론만 먼저**: **캐싱만으로는 커넥션 풀 병목이 전혀 풀리지 않았다.** A0→A1에서 요청당 SQL을 **100% 없앴는데도** 요청당 커넥션 대여와 `pending` 최대는 **다섯 시나리오 전부에서 미동도 하지 않았다**(대여 변화 +0.0%, `pending` 변화 0~1.1%). 풀이 풀린 것은 A1→A2(트랜잭션 분리)에서였고, 그 지점에서 대여와 `pending`이 동시에 0이 됐다.
>
> 그 결과 **포화 주체가 커넥션 풀에서 CPU로 옮겨갔다.** 캐싱은 병목을 없앤 것이 아니라 옮겼으며, 그 이동이 일어나려면 트랜잭션 분리가 함께 필요했다.
>
> **A0→A1이 처리량을 올린 메커니즘은 대기줄이 아니라 회전율이었다.** 대기줄 길이(`pending`)는 그대로인데 평균 커넥션 점유시간이 절반으로 줄어(D2 8.44ms → 4.92ms) 같은 풀이 더 빨리 회전했다. 줄을 짧게 만든 것이 아니라 줄이 빠지는 속도를 높인 것이다.
>
> ⚠️ 이 문서는 **중간 기록**이다. 규모 곡선(L1)·통계 오버헤드(O1)는 아직 측정하지 않았고, P3는 계획과 이탈이 있다(아래 "P3의 한계"). 반복 측정은 D2만 3회이고 나머지는 1회다.

## 측정 환경

| | 값 |
|---|---|
| App EC2 | **t3.small** (`i-06bc413840b95ca09`) |
| RDS | db.t3.micro, PostgreSQL **16.13** |
| ElastiCache | cache.t3.micro, Redis 7.1 |
| 부하 생성기 | k6 EC2 t3.micro (`i-0553d4c359e3caaa8`), k6 **v2.0.0** |
| 네트워크 | 전부 `ap-northeast-2a`, k6→App은 **프라이빗 IP**(`10.42.1.159`) |
| HikariCP | 풀 크기 **10** (기본값, 미설정) |
| Tomcat | maxThreads **200** (기본값, 미설정) |
| Spring 프로필 | **`prod`** — 매 run 기동 로그로 검증 |
| Hibernate 통계 | **상시 ON** (`generate_statistics: true`) |
| 시드 | `seed-benchmark.sql` + `seed-popular.sql` — upload_course 3,000건, `view_count = 3000 - id` |
| 코드 기준점 | 단일 커밋·단일 JAR. arm은 프로퍼티 2개 조합으로만 전환 |

**변수 통제**: 세 arm은 같은 JAR을 쓰고 `yourtrip.benchmark.upload-course-cache`(enabled/disabled)와 `.upload-course-tx`(separated/wrapped)만 바뀐다. 커밋 checkout 방식은 그 사이에 낀 CloudFront 전환·presign 개선이 함께 딸려와 "바뀐 변수가 하나"를 지킬 수 없어 택하지 않았다.

| arm | 캐시 | 트랜잭션 경계 | 대응하는 실제 코드 상태 |
|---|---|---|---|
| **A0** | 없음 | 메서드 전체 | 캐싱 도입 이전 |
| **A1** | 있음 | 메서드 전체 | `7e74d0d`/`604d3a4` ~ 트랜잭션 분리 직전 |
| **A2** | 있음 | Reader만 | **현재 운영 (기본값)** |

**전환 절차**는 [switch 스크립트](../../../scripts/loadtest/switch-arm.sh)와 동일한 순서를 자동화했다 — 프로퍼티 교체 → 재기동 → **활성 프로필 검증** → 재시딩(`DB_DDL_AUTO=create`라 재기동마다 스키마가 DROP/CREATE된다) → `FLUSHALL` → 랭킹 8키 워밍(캐시 arm만).

**지표 수집**: App EC2의 `/actuator/prometheus`를 **1초 간격으로 직접 폴링**했다(run당 350~370 샘플). 커넥션 대여 횟수는 `hikaricp_connections_usage_seconds_count`의 증분을 요청 수 증분으로 나눠 구했다 — `active`는 순간값이라 빌렸다 즉시 반납하는 캐시 히트 경로에서는 폴링 간격 사이에 잡히다 만다.

---

## P1 — 인기 코스 단독, 워밍 캐시 (ramping-vus 1→200, 450초)

랭킹 캐시 키 `ALL` 하나만 반복 조회. 캐시 arm에서는 100% 히트 조건이다.

| | 요청당 SQL | 요청당 **대여** | `pending` 최대 | `active` 최대 | `acquire` 최대 | 평균 점유 | TPS | avg | p95 | p99 | max | CPU 평균 | 실패율 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **A0** 캐싱없음 | **7.9926** | **0.9991** | **187** | 10 | 1.3062s | 8.81ms | 870.6/s | 62.9ms | 210.4ms | 394.9ms | 1367.1ms | 0.864 | 0% |
| **A1** 캐싱/분리전 | **0.0000** | **0.9996** | **189** | 10 | 1.0141s | 3.63ms | 1883.0/s | 29.0ms | 113.3ms | 216.6ms | 1020.0ms | 0.742 | 0% |
| **A2** 캐싱/분리후 | 0.0000 | **0.0000** | **0** | 0 | 0.0048s | — | 2621.4/s | 20.8ms | 61.4ms | 84.8ms | 203.6ms | 0.834 | 0% |
| **A0→A1** | **-100%** | **+0.1%** | **+1.1%** | 0 | -22.4% | -58.8% | **+116.3%** | -53.9% | **-46.1%** | -45.1% | -25.4% | | |
| **A1→A2** | 동일 | **-100%** | **-100%** | -10 | **-99.5%** | — | **+39.2%** | -28.3% | **-45.8%** | -60.9% | -80.0% | | |

### VU 단계별 (서버측 1초 폴링을 단계 창으로 슬라이스)

| VU | A0 TPS | avg | pend | A1 TPS | avg | pend | A2 TPS | avg | pend |
|---|---|---|---|---|---|---|---|---|---|
| 5 | 260.9 | 8.4ms | 0 | 602.6 | 3.4ms | 0 | 697.7 | 3.0ms | 0 |
| 10 | 794.8 | 8.0ms | 0 | 1655.4 | 3.5ms | 0 | 1723.7 | 3.3ms | 0 |
| **20** | 992.7 | 13.6ms | **8** | 2156.9 | 5.8ms | **9** | 2630.9 | 4.5ms | **0** |
| **50** | 1008.5 | 32.3ms | 38 | 2230.5 | 14.4ms | 38 | 3414.7 | 8.4ms | **0** |
| 100 | 993.1 | 71.3ms | 88 | 2154.9 | 32.9ms | 88 | 3362.7 | 17.7ms | 0 |
| 200 | 946.9 | 153.0ms | 187 | 2071.1 | 70.6ms | 189 | 2906.2 | 41.6ms | 0 |

| | 포화 시작 VU | 포화 주체 | 판정 근거 |
|---|---|---|---|
| **A0** | **20** | **커넥션 풀** | VU 10→20에서 TPS +24.9%(VU +100%) — 기준(50%) 미달. 같은 단계에서 `pending` 최초 발생 |
| **A1** | **20** | **커넥션 풀** | VU 10→20에서 TPS +30.3% — 기준 미달. `pending` 최초 발생도 동일 |
| **A2** | **50** | **CPU** | VU 20→50에서 TPS +29.8%(VU +150%). `pending` 0, tomcat busy 32.4/200, **CPU 평균 0.947** |

---

## P3 — 인기 코스, 고정 동시성 100 (constant-vus, 90초)

| | 요청당 SQL | 요청당 대여 | `pending` 최대 | `acquire` 최대 | 평균 점유 | TPS | avg | p95 | p99 | CPU 평균 |
|---|---|---|---|---|---|---|---|---|---|---|
| **A0** | **7.9828** | **0.9972** | **90** | 1.6091s | **39.70ms** | 244.7/s | 407.9ms | 765.1ms | 1016.2ms | 0.965 |
| **A1** | 0.0000 | **0.9994** | **90** | 1.2056s | 6.61ms | 1322.0/s | 75.4ms | 230.1ms | 387.1ms | 0.947 |
| **A2** | 0.0000 | **0.0000** | **0** | 0.0014s | — | 1745.6/s | 57.1ms | 110.5ms | 188.5ms | 0.954 |
| **A0→A1** | -100% | **+0.2%** | **0%** | -25.1% | -83.4% | **+440.2%** | -81.5% | **-69.9%** | -61.9% | |
| **A1→A2** | 동일 | **-100%** | **-100%** | **-99.9%** | — | +32.0% | -24.3% | **-52.0%** | -51.3% | |

**A0의 평균 커넥션 점유시간 39.70ms**가 눈에 띈다 — P1(8.81ms)의 4.5배다. 동시성 100이 즉시 걸리면서 10개 커넥션에 대기가 몰려, 각 커넥션이 붙잡혀 있는 시간이 길어진 결과다. `pending`이 90(= 동시성 100 − 풀 10)으로 딱 떨어지는 것도 같은 이야기다.

### P3의 한계 — 계획과의 이탈

계획의 P3는 "부하 중 **10초마다 `FLUSHALL` ×9**로 콜드 스탬피드를 반복 유발"이었는데, **실행 시 그 동시 FLUSHALL 루프를 넣지 않았다.** 결과적으로 A1·A2는 워밍된 캐시를 고정 동시성으로 때린 것이 되어, **계획이 의도한 "캐싱 최악 조건"을 측정하지 못했다.**

위 수치는 "고정 동시성 100에서의 3-arm 비교"로는 유효하지만, 콜드 스탬피드 하한이 아니다. **재실행이 필요하다.**

---

## P5 — 혼합 부하 (인기 100 req/s 배경 + 상세조회 VU 1→200, 330초)

운영에 가장 가까운 시나리오다. `/popular`은 고정 도착률로 배경을 깔고, 상세조회의 VU를 올려 포화점을 찾는다.

| | 요청당 SQL | 요청당 대여 | `pending` 최대 | `active` 최대 | `acquire` 최대 | 총 TPS | popular p95 | popular p99 | detail p95 | detail p99 | CPU 평균 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **A0** | 4.4500 | **0.9991** | **189** | 10 | 2.1620s | 880.5/s | 256.3ms | 488.8ms | 264.4ms | 497.8ms | 0.918 |
| **A1** | 0.0227 | **0.9994** | **189** | 10 | 1.8281s | 1607.1/s | 153.6ms | 297.2ms | 166.6ms | 310.2ms | 0.886 |
| **A2** | 0.0168 | **0.0042** | **0** | 2 | 0.0178s | 2167.4/s | 84.6ms | 124.9ms | 83.8ms | 120.2ms | 0.953 |
| **A0→A1** | -99.5% | **+0.0%** | **0%** | 0 | -15.4% | +82.5% | -40.1% | -39.2% | -37.0% | -37.7% | |
| **A1→A2** | -25.8% | **-99.6%** | **-100%** | -8 | **-99.0%** | +34.9% | **-44.9%** | **-58.0%** | **-49.7%** | **-61.3%** | |

### VU 단계별

| VU | A0 TPS | avg | pend | A1 TPS | avg | pend | A2 TPS | avg | pend |
|---|---|---|---|---|---|---|---|---|---|
| 20 | 560.2 | 19.2ms | 12 | 755.8 | 12.8ms | 9 | 884.2 | 9.9ms | 0 |
| **50** | 856.0 | 43.8ms | 47 | 1629.0 | 20.2ms | 41 | 2092.6 | 13.8ms | 0 |
| **100** | 1006.5 | 80.3ms | 99 | 1884.9 | 39.3ms | 95 | 2735.8 | 20.7ms | 0 |
| 200 | 969.3 | 164.0ms | 189 | 1877.4 | 81.9ms | 189 | 2467.1 | 47.1ms | 0 |

| | 포화 시작 VU | 포화 주체 |
|---|---|---|
| **A0** | **50** | 커넥션 풀 + CPU |
| **A1** | **100** | **커넥션 풀** |
| **A2** | **100** | **CPU** |

**A1의 요청당 SQL 0.0227**(총 12,028건)은 노이즈가 아니라 **상세 캐시의 콜드 채움**이다. 시드가 3,000코스이고 상세 미스 1건당 SQL이 4건이므로 3,000 × 4 = 12,000 — 실측 12,028과 거의 정확히 일치한다. Phase 0에서 구한 "미스당 SQL 상수"가 EC2에서도 성립함을 보여주는 교차 검증이다.

---

## D2 — 상세 조회 단독, 닫힌 루프 (ramping-vus 1→200, 450초)

`GET /api/upload-courses/{id}`를 id 2~3,000에 균등 무작위로 친다. P1과 다른 점은 히트율이 아니라 **키 분산 패턴**이다 — 랭킹 1키 + 아이템 MGET 배치(P1)가 아니라 3,000키에 흩어진 단일 GET이다.

**이 시나리오만 각 arm 3회 반복했다.** 아래 값은 중앙값이고 TPS·p95는 변동폭을 함께 적는다.

| | 요청당 SQL | 실측 히트율 | 요청당 **대여** | `pending` 최대 | `active` 최대 | `acquire` 최대 | 평균 점유 | TPS | avg | p95 | p99 | CPU 평균 | 실패율 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **A0** 캐싱없음 | **3.9963** | 0.1% | **0.9991** | **186** | 10 | 2.6870s | 8.44ms | 871.0/s<br>(841~873) | 62.9ms | 221.1ms<br>(219~228) | 419.3ms | 0.903 | 0% |
| **A1** 캐싱/분리전 | **0.0187** | **99.5%** | **0.9994** | **187** | 10 | 1.6279s | 4.92ms | 1426.0/s<br>(1393~1433) | 38.4ms | 142.7ms<br>(143~144) | 281.9ms | 0.756 | 0% |
| **A2** 캐싱/분리후 | 0.0148 | 99.6% | **0.0037** | **0** | 2 | 0.0396s | 5.33ms | 1801.8/s<br>(1760~1846) | 30.3ms | 82.2ms<br>(81~84) | 116.0ms | 0.820 | 0% |
| **A0→A1** | **-99.5%** | | **+0.0%** | **+0.5%** | 0 | -39.4% | -41.7% | **+63.7%** | -39.0% | **-35.4%** | -32.8% | | |
| **A1→A2** | *(주1)* | | **-99.6%** | **-100%** | -8 | **-97.6%** | +8.3% | **+26.4%** | -21.1% | **-42.4%** | -58.9% | | |

> **주1 — A1→A2의 SQL 감소(-20.9%)는 캐시 효과가 아니다.** 두 arm의 **총 SQL이 12,011건으로 동일**하고 분모(처리 요청 수)만 커진 결과다. 요청당 값만 보면 캐시가 더 잘 들은 것처럼 보이지만 실제로 실행된 쿼리 수는 같다. 요청당 지표를 arm 간에 비교할 때의 함정이라 명시해 둔다.

**arm 간 차이가 run 간 변동폭보다 한 자릿수 이상 크다.** 반복 3회의 폭이 A0 ±1.9% / A1 ±1.4% / A2 ±2.4%인 데 비해 arm 간 격차는 +63.7%와 +26.4%다. 이 차이는 노이즈가 아니다.

### 미스 수가 키 풀 크기와 정확히 일치한다

| run | 총 SQL | ÷ 4 = 미스 수 | 키 풀 |
|---|---|---|---|
| `d2-a1-r1` | 12,011 | 3,003 | 3,000 |
| `d3-a1-r1` | 12,007 | 3,002 | 3,000 |
| `d3-a2-r1` | 12,000 | **3,000** | 3,000 |

계획 §5의 히트율 역산은 "미스 1건당 SQL 4건이 상수"라는 전제에 기대는데, 실측 미스 수가 키 풀 크기와 소수점까지 맞는다. 전제가 성립할 뿐 아니라 **캐시가 run 중 축출 없이 cache-aside로 정확히 동작**했음도 함께 보여준다.

### VU 단계별 (각 arm r1, 서버측)

| VU | A0 TPS | avg | pend | A1 TPS | avg | pend | A2 TPS | avg | pend |
|---|---|---|---|---|---|---|---|---|---|
| 5 | 249.7 | 8.8ms | 0 | 356.6 | 6.0ms | 0 | 342.0 | 6.3ms | 0 |
| 10 | 839.7 | 7.1ms | 0 | 1347.3 | 4.3ms | 0 | 1342.5 | 4.2ms | 0 |
| **20** | 909.4 | 14.2ms | **9** | 1689.6 | 7.5ms | **8** | 1938.4 | 6.2ms | **0** |
| **50** | 1015.0 | 31.6ms | 38 | 1653.9 | 19.5ms | 39 | **2306.0** | 13.1ms | **0** |
| 100 | 972.2 | 73.6ms | 89 | 1699.0 | 42.5ms | 87 | 2172.0 | 30.3ms | 0 |
| 200 | 879.2 | 164.8ms | 187 | 1546.1 | 94.5ms | 189 | 2273.0 | 54.9ms | 0 |

| | 포화 시작 VU | 포화 주체 | 판정 근거 |
|---|---|---|---|
| **A0** | **20** | 커넥션 풀 + **CPU** | VU 10→20에서 TPS +8.3%(VU +100%) — 기준 미달. `pending` 최초 발생(9), **CPU 평균 0.979** |
| **A1** | **20** | **커넥션 풀** | VU 10→20에서 TPS +25.4% — 기준 미달. `pending` 최초 발생(8). CPU 0.803으로 여유 |
| **A2** | (20) | 앱 밖 | 아래 주석 참고. `pending` 0, tomcat busy 13.9/200, CPU 0.805 — 셋 다 한계 아님 |

### A2의 knee 판정 — 규칙 출력(20)과 처리량 최대(50)는 서로 다른 것을 가리킨다

**규칙은 정의대로 동작했다. 기록 오류가 아니다.** D2 A2는 VU를 2배로 올렸는데 TPS가 1.44배만 늘었고, 이는 "TPS 증가율 < VU 증가율의 50%"라는 기준을 만족한다. **수익 체감이 VU 20에서 시작된 것은 사실이다.**

문제는 그 출력을 P1과 나란히 놓고 읽을 때 생긴다.

| VU 구간 | P1 A2 | D2 A2 |
|---|---|---|
| 10→20 | 1,723.7 → 2,630.9 (**+52.6%** — 문턱 통과) | 1,342.5 → 1,938.4 (**+44.4%** — 문턱 미달) |
| 20→50 | 2,630.9 → 3,414.7 (+29.8%) | 1,938.4 → 2,306.0 (+19.0%) |
| 50→100 | 3,414.7 → 3,362.7 (**-1.5%**) | 2,306.0 → 2,172.0 (**-5.8%**) |

**두 곡선의 거동은 사실상 같다** — 둘 다 VU 50에서 최대에 도달하고 그 뒤로 꺾인다. 그런데 같은 VU 구간의 증가율이 **8.2%p 차이로 문턱을 사이에 두고 갈려** 규칙 출력이 20과 50으로 달라진다. **이 출력만 비교해 "D2에서는 포화 VU가 안 밀렸다"고 읽으면 틀린 결론이 나온다.**

두 값은 서로 다른 것을 가리킨다:

| 값 | 의미 |
|---|---|
| **VU 20** | 수익 체감이 시작되는 지점 (규칙이 재는 것) |
| **VU 50** | 처리량이 최대에 도달하는 지점 (2,306 TPS, 이후 하락) |

처리량 최대 지점으로 읽으면 D2도 **A0 20 / A1 20 / A2 50**으로 P1과 같은 H5 패턴이다(A0는 VU 50에서 1,015 TPS로 정점 뒤 하락하나 20→50 증가폭이 이미 +11.7%에 그친다, A1은 20→50에서 **-2.1%**, A2만 50까지 계속 오른다).

**규칙을 사후에 고치지는 않는다.** 계획대로의 출력과 함께, 그 출력을 P1과 직접 비교할 수 없는 이유를 남긴다. 계획이 이미 명시한 해상도 한계("단계가 5/10/20/50/100/200이라 knee는 구간까지만 좁혀진다")가 실제로 문제가 된 사례다.

**VU 20에서 앱 안의 어떤 자원도 한계에 있지 않다** — `pending` 0, Tomcat busy 13.9/200(7%), CPU 0.805. P1 A2가 걸린 VU 50에서 CPU가 0.947이었던 것과 대비된다. 다만 **Lettuce(Redis) 계측이 없어 앱 밖에서 실제로 포화가 일어났을 가능성은 배제하지 못한다.** 그래서 포화 주체를 "앱 밖"으로 적었을 뿐, 포화가 없었다고 단정하지는 않는다.

A2가 계측 가능한 자원의 한계에 닿는 곳은 VU 200이고 그 단계의 CPU 평균이 0.938이다 — P1과 같이 **CPU가 최종 천장**이다.

### A0의 CPU가 인기 코스보다 높다 (원인 미확정)

D2 A0는 **요청당 SQL이 P1 A0의 절반(4 vs 8)인데 CPU는 더 높다.**

| VU | P1 A0 TPS / CPU | D2 A0 TPS / CPU |
|---|---|---|
| 10 | 794.8 / 0.744 | 839.7 / **0.962** |
| 20 | 992.7 / **0.892** | 909.4 / **0.979** |

비슷한 TPS에서 상세 조회가 CPU를 더 쓴다. **원인은 이번 계측으로 확정하지 못했다.** 응답 페이로드가 더 크다는 점(상세는 일정·장소·이미지 10개, 인기는 코스 5건 요약)이 후보지만, 프로파일링 없이는 엔티티 하이드레이션·JSON 직렬화 중 어디인지 가릴 수 없다.

이 차이가 **포화 주체 라벨을 가른다는 점은 유의해야 한다** — P1 A0는 0.892로 CPU 문턱(0.9) 아래라 "커넥션 풀", D2 A0는 0.979로 위라 "커넥션 풀 + CPU"가 됐다. **문턱을 사이에 두고 0.09 차이로 갈린 라벨이라 견고하지 않다.**

> CloudFront 서명은 이 차이의 원인이 아니다. `uploadcourse`의 이미지 URL은 [`getPublicUrl`](../../../src/main/java/backend/yourtrip/global/cloudfront/service/CloudFrontService.java:58)로 **서명 없이 문자열만 조합**한다(서명은 `private/` 스코프인 `mycourse` 전용이다). 서명 경로를 타더라도 알고리즘은 RSA-2048이 아니라 **ECDSA P-256**으로 전환돼 있다.

---

## D3 — 상세 조회, 열린 루프 (ramping-arrival-rate → 1,200 req/s, 300초)

닫힌 루프의 역설을 피하기 위한 시나리오다. D2(ramping-vus)는 VU가 응답을 받아야 다음 요청을 보내므로 **서버가 빨라질수록 제공 부하도 함께 늘어나** "초당 N건을 제공했을 때 몇 %를 처리하는가"를 직접 관측할 수 없다. D3는 도착률을 고정해 그것을 직접 잰다.

**TPS는 arm 간 비교 지표가 아니다** — 제공 도착률이 정하므로 세 arm이 비슷하게 나온다. 여기서 읽어야 할 것은 **지연·배압·포화 도착률**이다.

| | 요청당 SQL | 실측 히트율 | 요청당 **대여** | `pending` 최대 | 평균 점유 | 서버 TPS | p95 | p99 | `dropped_iterations` | tomcat busy 최대 | CPU 평균 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **A0** 캐싱없음 | **3.9875** | 0.3% | **0.9968** | **189** | 7.24ms | 260.5/s | **1258.7ms** | 1602.3ms | **4,352** | **200/200** | 0.387 |
| **A1** 캐싱/분리전 | **0.1468** | **96.3%** | **0.9971** | **189** | 3.85ms | 273.4/s | 50.5ms | 534.5ms | 760 | **200/200** | 0.288 |
| **A2** 캐싱/분리후 | 0.1461 | 96.3% | **0.0365** | **0** | 5.57ms | 274.9/s | **14.9ms** | 261.8ms | **305** | **40/200** | 0.269 |
| **A0→A1** | **-96.3%** | | **+0.0%** | **0%** | -46.8% | +5.0% | **-96.0%** | -66.6% | -82.5% | 0% | |
| **A1→A2** | -0.5% | | **-96.3%** | **-100%** | +44.7% | +0.5% | **-70.6%** | -51.0% | -59.9% | **-80%** | |

제공된 총 요청은 82,800건(도착률 곡선의 적분)이고, 실제 처리량은 A0 78,512건(94.8%) / A1 81,768건(98.8%) / A2 82,156건(**99.2%**)이다.

### 포화 도착률

10초 버킷으로 잘라 **측정 TPS가 그 시점 제공 도착률의 90%에 못 미치는 지점**을 포화로 본다. 제공 도착률이 마지막 구간에서 200→1,200으로 선형 상승하므로 단계 평균으로는 knee 위치를 짚을 수 없다.

| arm | 포화 도착률 | 성격 |
|---|---|---|
| **A0** | **~922 req/s** | 확정 — `t=270`부터 `pend` 189 고정, `busy` 200/200, 평균 지연 8.6ms → 106ms → 233ms로 붕괴 후 **회복 없음** |
| **A1** | **≥1,144 req/s** | **하한값** — 마지막 버킷에서 처음 미달(83%)했고 거기서 run이 끝나 연속 확인 구간이 없다 |
| **A2** | **미포화 (>1,144 req/s)** | 제공 1,144 req/s에서 충족률 95%, `pend` 0, CPU 0.552, 워커 11개 — **상한을 찾지 못했다** |

**판정에는 연속 2버킷 미달을 요구했다.** k6가 마지막 구간에 진입하며 VU를 새로 할당할 때 한 버킷만 충족률이 떨어졌다가 곧바로 회복하는 일시적 정체가 있는데(실측 65% → 97%), 이를 포화로 세면 A0의 포화 도착률이 922 req/s가 아니라 **256 req/s로 3.6배 과소평가된다.** 회복하는 것은 포화가 아니다.

### Tomcat 워커 점유 — 대기가 어디에 쌓였는지의 직접 증거

D3에서 가장 선명하게 드러난 지표다. **A0·A1은 워커 200개를 전부 소진했는데 A2는 40개만 썼다.**

같은 부하를 더 잘 처리하면서 스레드 점유가 1/5이라는 것은, A0·A1의 워커 대부분이 **일하고 있던 게 아니라 커넥션을 기다리며 묶여 있었다**는 뜻이다. HikariCP 풀 10 : Tomcat maxThreads 200이라는 20:1 구조에서 초과분 190개가 갈 곳은 `pending` 큐뿐이다.

A1의 p95가 1,258.7ms → 50.5ms로 96% 줄어든 것도 대기줄 **길이**(`pending` 189, A0와 동일) 때문이 아니라 줄이 빠지는 **속도**(평균 점유 7.24ms → 3.85ms) 때문이다.

---

## 판정

### 판정 1 — 캐싱만으로는 풀 병목이 풀리지 않는다 (가설 H2)

**다섯 시나리오 전부에서 A0→A1의 커넥션 대여와 `pending`이 사실상 변하지 않았다.**

| 시나리오 | 요청당 SQL | 요청당 대여 | `pending` 최대 |
|---|---|---|---|
| P1 | 7.9926 → **0.0000** | 0.9991 → **0.9996** | 187 → **189** |
| P3 | 7.9828 → **0.0000** | 0.9972 → **0.9994** | 90 → **90** |
| P5 | 4.4500 → **0.0227** | 0.9991 → **0.9994** | 189 → **189** |
| **D2** | 3.9963 → **0.0187** | 0.9991 → **0.9994** | 186 → **187** |
| **D3** | 3.9875 → **0.1468** | 0.9968 → **0.9971** | 189 → **189** |

**SQL을 100% 없앴는데 커넥션은 요청마다 그대로 대여된다.** 커넥션을 잡는 조건이 "DB 쿼리를 실행하는가"가 아니라 **"트랜잭션을 여는가"** 이기 때문이다 — `provider_disables_autocommit`이 설정돼 있지 않아 트랜잭션 begin 시점에 물리 커넥션을 잡아 autocommit을 끄고, `readOnly = true`면 `Connection.setReadOnly(true)` 호출도 커넥션을 요구한다.

이것이 `popular-ramping.js:18-19`가 결정적 증거 형태로 지목한 **"일은 하나도 안 하면서 커넥션만 점유"** 다.

**두 엔드포인트(인기·상세)와 두 부하 모델(닫힌·열린 루프)에서 모두 재현됐다.** 대여 변화는 다섯 번 모두 **+0.0%**다.

### 판정 2 — 풀은 A1→A2에서만 풀린다 (가설 H3)

다섯 시나리오 전부에서 요청당 대여와 `pending`이 동시에 0이 됐고, `acquire` 최대는 97% 이상 줄었다(P1 1.0141s → 0.0048s, D2 1.6279s → 0.0396s).

### 판정 3 — 병목이 사라진 게 아니라 옮겨갔다 (가설 H5)

| 시나리오 | A0 | A1 | A2 |
|---|---|---|---|
| P1 | VU 20 / **커넥션풀** | VU 20 / **커넥션풀** | **VU 50 / CPU** |
| P5 | VU 50 / 커넥션풀+CPU | VU 100 / **커넥션풀** | VU 100 / **CPU** |
| **D2** | VU 20 / 커넥션풀+CPU | VU 20 / **커넥션풀** | 규칙 **20** / 처리량 최대 **50** — 앱 밖 → CPU *(주2)* |
| **D3** | **922 req/s** / 커넥션풀 | ≥1,144 req/s / **커넥션풀** | **미포화** (>1,144 req/s) |

> **주2** — D2 A2만 규칙 출력과 처리량 최대 지점이 갈린다. 규칙은 정의대로 VU 20을 짚었으나(TPS +44.4% < 문턱 50%), P1 A2는 같은 구간이 +52.6%로 문턱을 넘어 VU 50에서 걸렸다. **8.2%p 차이가 문턱을 사이에 두고 갈린 것이라 두 출력을 직접 비교할 수 없다** — 두 곡선 모두 VU 50에서 최대에 도달하고 그 뒤 꺾인다. 근거는 위 "A2의 knee 판정" 절에 있다.

**포화 주체가 네 시나리오 전부에서 커넥션 풀 → CPU(또는 앱 밖)로 바뀌었다.** 캐싱은 병목을 없앤 것이 아니라 **옮겼고**, 그 이동이 일어나려면 트랜잭션 분리가 함께 필요했다.

포화 지점도 밀렸다 — 닫힌 루프는 P1 20→50, P5 50→100이고 D2는 처리량 최대 지점으로 읽을 때 20→50이다(규칙 출력으로는 20→20이라 위 주2의 단서가 붙는다). 열린 루프(D3)에서는 A0가 922 req/s에서 무너진 데 반해 **A2는 1,144 req/s에서도 포화하지 않았다.**

### 판정 4 — 처리량 개선의 배분은 예측과 달랐다 (가설 H4, **예측 빗나감**)

계획은 "처리량·지연 개선의 **대부분이 A1→A2에서** 나온다"고 예측했으나, 실제로는 **A0→A1이 더 컸다**(P1 +116.3% vs +39.2%).

이는 가설의 메커니즘이 틀린 것이 아니라 **A0의 조건이 계획 가정보다 무거웠기 때문이다.** 계획은 A0의 요청당 SQL을 2건으로 가정했는데, Phase 0에서 드러난 **`travelCourse`·`user` LAZY 접근 N+1** 때문에 실제로는 8건이었다. 캐싱이 없앤 CPU·DB 비용이 가정의 4배였던 것이다.

**풀 병목 관점에서는 계획대로 A1→A2에서만 풀렸다**(판정 1·2). 처리량 배분과 풀 병목 해소를 구분해서 읽어야 한다.

### 판정 5 — 기존 실측을 재현한다 (setup 검증)

계획의 검증 방법 2는 "A1→A2가 기존 실측의 `pending` 187→0 패턴을 재현하는가"였다.

| 구간 | 이번 측정 | 대응하는 기존 실측 |
|---|---|---|
| 인기 코스 (P1) | `pending` **189 → 0**<br>TPS 1,883 → 2,621 (**+39.2%**) | `popular-tx-separation` S1<br>`pending` **187 → 0**, TPS 2,048 → 2,684 (**+31.0%**) |
| **상세 조회 (D2)** | `pending` **187 → 0**<br>TPS 1,426 → 1,802 (**+26.4%**) | `stage0/production/ec2-rds.md`<br>`pending` **187 → 0**, TPS 1,068 → 1,552 (**+45.4%**) |

**두 엔드포인트 모두에서 `pending` 187→0 패턴이 재현됐다.** 상세 조회는 이번에 처음 A1→A2 구간을 같은 setup으로 다시 잰 것인데, 기존 실측과 같은 형태가 나왔다.

절대값 차이는 예상된 것이다 — 기존 상세조회 실측은 App EC2가 `t3.micro`였고 `seed-popular.sql` 이전 시드였으며, 이번 측정은 `generate_statistics`가 상시 ON이다. TPS 증가폭이 +45.4% → +26.4%로 작아진 것도 **A1의 출발선이 높아졌기 때문**이다(1,068 → 1,426). 이 setup이 과거 실측과 동등한 조건을 만들고 있음을 뒷받침한다.

### 판정 6 — A0→A1이 처리량을 올린 것은 대기줄이 아니라 회전율이다

판정 1이 "`pending`이 안 변한다"인데도 A0→A1에서 처리량이 오르는 것(D2 +63.7%, P1 +116.3%)이 모순처럼 보인다. 두 지표를 함께 놓으면 풀린다.

| | 요청당 대여 | `pending` 최대 | **평균 커넥션 점유시간** | TPS |
|---|---|---|---|---|
| D2 A0 | 0.9991 | 186 | **8.44ms** | 871.0/s |
| D2 A1 | 0.9994 | 187 | **4.92ms** | 1426.0/s |

**대기줄 길이는 그대로인데 점유시간이 절반이 됐다.** 트랜잭션 안에서 실행하던 SQL 4건이 사라져 같은 커넥션을 더 빨리 반납하고, 풀 10개가 더 빨리 회전한다. 줄을 짧게 만든 것이 아니라 **줄이 빠지는 속도를 높인** 것이다.

이것이 왜 캐싱만으로 부족한지를 설명한다 — 회전율을 아무리 올려도 **풀 10개라는 상한과 대기 자체는 남는다.** D3에서 A1이 워커 200개를 여전히 전부 소진한 것(A2는 40개)이 그 잔존 대기의 크기를 보여준다.

---

## 한계

- **반복 측정은 D2만 3회다.** P1·P3·P5·D3는 각 arm 1회라 분산을 잡지 못했다. 계획은 P1·D2·P5를 각 3회로 잡았다.
- **P3가 계획과 이탈했다** — 동시 `FLUSHALL` 루프 누락으로 콜드 스탬피드를 측정하지 못했다(위 "P3의 한계").
- **규모 곡선(L1), 통계 오버헤드(O1)는 측정하지 않았다.**
- **D3에서 A2의 포화점을 찾지 못했다.** `MAX_RATE=1200`으로는 부족했다 — 제공 1,144 req/s에서 `pending` 0, CPU 0.552, 워커 11/200으로 여유가 크다. **"A2 ≥ 1,144 req/s"까지만 말할 수 있다.** A1도 마지막 버킷에서 처음 미달해 하한값이다. 상한을 알려면 `MAX_RATE`를 올려 재측정해야 한다.
- **knee 규칙의 50% 문턱이 시나리오 간 비교를 깨뜨린다.** D2 A2(+44.4%)와 P1 A2(+52.6%)는 거동이 사실상 같은데(둘 다 VU 50 최대) **8.2%p 차이가 문턱을 사이에 두고 갈려** 출력이 20과 50으로 달라진다. 규칙이 틀린 게 아니라 "수익 체감 시작"과 "처리량 최대"라는 **서로 다른 두 지점을 하나의 값으로 보고하는 구조** 때문이다. 시나리오 간 비교에는 곡선을 함께 봐야 한다.
- **D2 A2가 VU 20에서 실제로 포화했는지 확인할 수단이 없다.** 그 지점에서 `pending` 0, Tomcat busy 7%, CPU 0.805로 **앱 안의 자원은 어느 것도 한계가 아니다.** 앱 밖(Redis/네트워크/부하생성기)의 포화 가능성은 Lettuce 계측 부재로 배제하지 못한다.
- **포화 주체 라벨도 문턱에 민감하다.** P1 A0(CPU 0.892)와 D2 A0(0.979)는 0.9 문턱을 사이에 두고 "커넥션 풀" / "커넥션 풀 + CPU"로 갈렸다. 0.09 차이로 갈린 라벨이라 견고하지 않다.
- **상세 조회가 인기 코스보다 요청당 CPU를 더 쓰는 원인을 확정하지 못했다.** SQL은 절반(4 vs 8)인데 CPU는 높다. 응답 페이로드 크기가 후보지만 프로파일링 없이는 가릴 수 없다.
- **스케줄러 틱이 모든 D2·D3 run에 1~2회 들어왔다.** 다만 arm별 분포가 A0 = 1·1·2, A1 = 1·1·2, A2 = 2·1·1로 균등해 arm 간 편향은 없다. Phase 1에서는 `p5-a2-r1`만 1회였다.
- **Lettuce(Redis) 계측이 없다.** D2 A2의 포화 주체가 "앱 밖"으로 판정됐는데, 그 정체가 Redis인지 네트워크인지 부하 생성기인지 **이번 계측으로는 가릴 수 없다.** Redis 풀 8개 : Tomcat 200스레드 구조라 Redis가 유력한 후보지만 확인/배제하지 못했다. 이 공백이 다음 과제다.
- **서버측 percentile 히스토그램이 없어** 단계별로는 평균 지연만 얻었다. p95/p99는 run 전체에 대한 k6 클라이언트 측정이다.
- **A2의 CPU 평균이 P1 0.834 / P3 0.954 / P5 0.953 / D2 0.820으로 이미 높다.** t3.small(vCPU 2개)에서 1,800~2,600 TPS를 처리한 결과이고, 더 큰 인스턴스에서는 포화 VU가 더 밀릴 가능성이 있다.
- **요청당 지표를 arm 간에 비교할 때 분모가 다르다.** D2 A1→A2의 "SQL -20.9%"는 총 SQL이 12,011건으로 동일하고 처리 요청 수만 늘어난 결과다(위 D2 주1). 캐시 효과가 아니다.

## 운영 중 겪은 사고 (재실행 시 주의)

측정 자동화에서 **배치 두 개가 동시에 실행돼 3개 run을 폐기**했다. 원인이 연쇄적이라 기록해 둔다.

1. **자기 자신을 매칭한 대기 조건.** 배치 완료를 "로그에서 특정 문자열 세기"로 판정했는데 대기 메시지에 그 문자열을 넣어, 자기가 쓴 줄이 자기 조건을 만족시켜 즉시 통과했다.
2. **분리된 프로세스가 죽지 않았다.** 상위 태스크를 중단해도 손자 프로세스가 살아남았고, Git Bash의 `pkill -f`가 Windows 프로세스를 잡지 못해 "정리됨"이라는 잘못된 확인을 받았다. PowerShell로 PID 트리를 종료해야 했다.
3. **동시 arm 전환이 앱을 죽였다.** 두 배치가 각자 `/opt/app/.env`에 같은 키를 append했고, **spring-dotenv가 중복 키에서 `IllegalStateException`을 던져** 앱이 67회 재시작 루프에 빠졌다.

대응으로 (a) 배치에 `mkdir` 기반 원자적 락을 넣어 동시 실행을 차단하고, (b) 체인 구조를 폐기해 단일 배치로 통합했으며, (c) arm 전환마다 `.env` 중복 키를 제거하는 스크립트를 App EC2에 상주시켰다.

**증상이 "앱이 연결을 거부한다"로 나타나 처음에는 부하로 인한 포화로 오인했다.** `systemctl` 상태가 `143`(SIGTERM)인 것을 보고서야 외부에서 재기동시켰음을 알았다 — 크래시와 의도적 재기동을 구분하는 데 종료 코드가 결정적이었다.

### 세션을 나눠 재개할 때 — 퍼블릭 IP 두 종류가 모두 바뀐다

Phase 2를 별도 세션으로 재개하면서 **접속이 전부 막혔다.** 원인이 두 가지가 겹쳐 있었다.

1. **EC2의 퍼블릭 IP가 바뀐다.** stop/start마다 재할당되므로 측정 하네스의 대상 주소를 갱신해야 한다. 프라이빗 IP는 그대로다.
2. **개발 머신의 공인 IP도 바뀐다.** 보안그룹의 SSH·8080 인바운드 규칙이 옛 IP를 가리켜 **SSH와 Prometheus 폴링이 동시에 막혔다.** 이 규칙 3개는 `var.my_ip_cidr`을 쓰는 **terraform 관리 대상**이라 CLI로 직접 고치면 drift가 된다. `terraform.tfvars`를 고치고 `-target`으로 세 규칙만 apply하는 것이 맞다(`3 added, 3 destroyed` — SG 규칙은 불변 리소스라 CIDR 변경이 replace로 표시되며, 인스턴스는 건드리지 않는다).

하네스에서는 **퍼블릭 IP가 두 스크립트에 각각 하드코딩돼 있어** 한쪽만 갱신하면 죽은 IP로 ssh를 시도하는 구조였다. 재개 전에 한 곳으로 모았다.

## 인프라 정리

- ⚠️ **terraform user_data drift가 남아 있다.** dev 머지가 `templates/app-user-data.sh.tpl`에 `SPRING_PROFILES_ACTIVE=prod`를 추가했고 `user_data_replace_on_change = true`라, 다음 `terraform apply` 시 **App 인스턴스가 교체된다**(배포된 JAR·CloudFront 개인키 유실). 이번에도 `/opt/app/.env`를 직접 고쳐 우회했고, 보안그룹 규칙만 `-target`으로 적용했다.
- 측정용 개발 IP 보안그룹 규칙 3개는 아직 회수하지 않았다(현재 `220.65.239.41/32`).
- `terraform.tfstate`·`terraform.tfvars`가 이번 apply로 바뀌었다. **메인 워킹트리 사본과 동기화가 필요하다**([worktree.md](../../guide/worktree.md)) — 규칙 회수까지 끝낸 뒤 한 번에 맞춘다.
- RDS·ElastiCache는 관행대로 유지(정지 불가/제약)하며 **과금이 계속된다.**

## 참고 문서

- [phase0-local-gate.md](phase0-local-gate.md) — 토글 검증과 미스 경로 N+1 발견
- [README.md](README.md) — arm 설계와 진행 상황
- [popular-tx-separation/ec2-measurement.md](../popular-tx-separation/ec2-measurement.md) — A1→A2 구간의 선행 실측
- [guide/ec2-rds-loadtest.md](../../guide/ec2-rds-loadtest.md) — EC2 분리 환경 실행 절차
